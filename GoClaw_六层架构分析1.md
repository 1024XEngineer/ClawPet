# GoClaw 六层模块架构分析

> 基于业务闭环拆分：被看见 → 被理解 → 被决策 → 被执行 → 被反馈 → 被沉淀

---

## 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  第一层：表现层          用户看到的桌宠本体               │
├─────────────────────────────────────────────────────────┤
│  第二层：交互承接层      所有输入的入口                   │
├─────────────────────────────────────────────────────────┤
│  第三层：陪伴与关系层    情感系统核心，无执行能力          │
├─────────────────────────────────────────────────────────┤
│  第四层：决策与编排层    产品大脑，任务识别与路由          │
├─────────────────────────────────────────────────────────┤
│  第五层：执行能力层      真正帮用户动手                   │
├─────────────────────────────────────────────────────────┤
│  第六层：安全与沉淀层    让产品可信、越用越有价值          │
└─────────────────────────────────────────────────────────┘
```

---

## 第一层：表现层（前端）

### 这一层解决什么问题

用户怎么感知它是「一个角色」而不是后台程序。

所有的情感价值，最终都要通过这一层被用户感知到——角色的表情、动作、声音、气泡里的一句话，是用户与产品建立情感连接的唯一通道。这一层做不好，后面所有层的设计都白费。

### 包含什么内容 & 怎么解决

| 模块 | 内容说明 | 技术实现 |
|------|----------|---------|
| **Live2D 角色形象** | 角色立绘，透明背景，常驻桌面右下角 | Electron 透明窗口 + Cubism SDK 4 + PixiJS |
| **表情与动作** | 4种基础表情（neutral/happy/focused/worried），多套动作（待机/工作/邀功/陪伴） | Live2D 表情参数由后端 WebSocket 实时推送驱动 |
| **语音播报** | 角色专属声线，任务汇报和主动关心有语音 | Fish Speech 本地 TTS（V2）|
| **悬浮面板** | Always-on-Top，可拖拽，右键菜单 | Electron BrowserWindow 配置 |
| **对话气泡** | 角色左上方弹出，自动消失，支持换行 | Electron Renderer 层实现 |
| **任务进度气泡** | 执行中实时显示「已整理 X / Y…」 | WebSocket 推送进度事件驱动 |
| **任务确认弹窗** | 中风险任务展示操作预览，等待用户确认 | 独立弹窗组件 |
| **剧情 / 图鉴界面** | 亲密度阶段解锁的剧情内容展示 | 独立面板（V3）|
| **权限 / 设置界面** | 感知权限开关、记忆管理、操作日志 | 独立 Electron 窗口 |

### 与 PicoClaw 的关系

| 内容 | 策略 | 原因 |
|------|------|------|
| 基础聊天 UI 参考 | 复用 PicoClaw | `web/frontend` 的聊天界面逻辑可参考，不直接使用 |
| Live2D 渲染 | **GoClaw 新建** | PicoClaw 无任何渲染能力 |
| 透明桌面窗口 | **GoClaw 新建** | Electron 特有，PicoClaw 完全没有 |
| 对话气泡 / 进度气泡 | **GoClaw 新建** | 桌宠专属交互形态 |
| 任务确认弹窗 | **GoClaw 新建** | 桌宠安全确认流程 |
| 设置面板 | **改造 PicoClaw** | PicoClaw `web/backend` 有配置界面，改造为桌宠设置用 |

---

## 第二层：交互承接层（用户信息输入）

### 这一层解决什么问题

用户怎么把事交给它，它又怎么在合适的时候出现。

这一层决定交互的自然程度。用户越不需要「切换模式」去使用桌宠，体验越好——理想状态是用户直接开口说话，或者桌宠已经感知到用户需要帮助而主动出现。

### 包含什么内容 & 怎么解决

| 模块 | 内容说明 | 技术实现 |
|------|----------|---------|
| **文本输入** | 点击角色弹出输入框，回车发送 | Electron Renderer + WebSocket 发给 Go 后端 |
| **语音输入** | 唤醒词触发，说话自动转文字 | Porcupine 唤醒词 + Whisper 本地 ASR（V2）|
| **拖拽输入** | 拖拽文件到角色上触发相关任务 | Electron drag & drop API（V3）|
| **点击唤醒** | 点击角色触发对话，双击触发菜单 | Electron 鼠标事件监听 |
| **主动事件唤醒** | 角色根据感知状态主动弹出气泡 | 主动触发引擎推送 WebSocket 事件 |
| **紧急制动 / 中断** | 快捷键暂停所有执行权限 | 全局键盘钩子（Electron globalShortcut）|
| **屏幕场景感知** | 识别用户当前在做什么（写代码/写文档/游戏/空闲）| Go 后端轮询系统 API 获取活跃窗口标题，5s一次 |
| **键盘活跃度感知** | 判断用户是否在专注工作，避免误打扰 | Go 后端统计5s内击键次数（只计频率，不记内容）|
| **Desktop WebSocket 频道** | 前后端通信总线，承载所有事件 | Go `pkg/channels/desktop`，替代 PicoClaw 的 Telegram 频道 |

### 与 PicoClaw 的关系

| 内容 | 策略 | 原因 |
|------|------|------|
| 消息收发逻辑 | **改造 PicoClaw** | PicoClaw `pkg/channels` 的消息总线可复用，替换底层传输为 WebSocket |
| 语音 ASR | **复用 PicoClaw** | PicoClaw 已接入 Groq Whisper，V2 直接复用 |
| Desktop WebSocket 频道 | **GoClaw 新建** | PicoClaw 只有 Telegram/Discord/WhatsApp，无桌面本地频道 |
| 屏幕场景感知 | **GoClaw 新建** | PicoClaw 完全没有，新建 `pkg/scene` |
| 键盘活跃度感知 | **GoClaw 新建** | PicoClaw 完全没有，归入 `pkg/scene` |
| 唤醒词检测 | **GoClaw 新建** | V2，接入 Porcupine，归入 `pkg/scene` |
| 紧急制动 | **GoClaw 新建** | 安全需求，归入 `pkg/risk` |

---

## 第三层：陪伴与关系层（情感系统核心）

### 这一层解决什么问题

它为什么像「伴侣 / 伙伴」，而不是一次性聊天工具。

这是 GoClaw 区别于所有现有产品的核心差异层。用户在这一层感受到的是：「他记得我」「他了解我」「我们的关系在慢慢变好」。没有这一层，桌宠就只是一个会动的聊天框。

### 包含什么内容 & 怎么解决

| 模块 | 内容说明 | 技术实现 |
|------|----------|---------|
| **人设与语气** | 角色性格、说话风格、口头禅，始终一致 | `workspace/SOUL.md` + `IDENTITY.md` 注入 System Prompt |
| **伴侣记忆** | 短期（当日，次日过期）/ 长期（重要事件，永久）/ 偏好（习惯喜好）三种记忆 | 本地 SQLite + LLM 自动提取评分 |
| **Recall 触发** | 下次对话时自然提起上次说过的事 | 相似度检索（V1 关键词，V2 向量）注入 Prompt |
| **亲密度系统** | 0-3000+ 四阶段，影响语气和解锁内容 | Go 状态机，每次有效互动后累加 |
| **情绪状态机** | 心情值 / 精力值，事件驱动，推动 Live2D 表情变化 | Go 内部状态机，变化时推送 WebSocket |
| **主动关心** | 感知用户状态后主动开口（深夜工作、完成大任务等） | 主动触发引擎（30s 轮询 + 规则引擎 + 冷却管理）|
| **叙事剧情库** | 随亲密度解锁的角色背景故事和特殊事件 | 本地文件存储（V3）|
| **碎片化叙事** | 日常对话中自然透露角色的过去和喜好 | 角色人设文件 + 叙事触发规则（V3）|

### 与 PicoClaw 的关系

| 内容 | 策略 | 原因 |
|------|------|------|
| 人设与语气 | **复用 PicoClaw** | `workspace/SOUL.md` `IDENTITY.md` 直接使用 |
| 记忆系统（基础存储）| **复用 PicoClaw** | `pkg/memory` + `workspace/memory/MEMORY.md` 可用 |
| 记忆系统（结构化扩展）| **改造 PicoClaw** | 加重要性评分、过期时间、recall 计数，改为 SQLite 存储 |
| 亲密度系统 | **GoClaw 新建** | PicoClaw 无此概念，新建 `pkg/emotion` |
| 情绪状态机 | **GoClaw 新建** | PicoClaw 无此概念，归入 `pkg/emotion` |
| 主动触发引擎 | **改造 PicoClaw** | PicoClaw `pkg/heartbeat` 是 30 分钟周期任务，改造为场景感知规则引擎 `pkg/proactive` |
| 叙事剧情库 | **GoClaw 新建** | PicoClaw 无此概念，V3 新建 |
| 日常节奏管理 | **改造 PicoClaw** | PicoClaw `pkg/cron` 有定时任务，扩展加时段判断和纪念日追踪 |

---

## 第四层：决策与编排层（产品大脑）

### 这一层解决什么问题

当前到底该聊天、关心、澄清，还是去做任务。

每一句用户输入都需要被「理解」：这是在倾诉情绪、在闲聊、还是在下达任务？同一句「我好累」，在深夜工作 3 小时后和在周末午后，应该触发完全不同的反应。这一层是产品的中枢神经。

### 包含什么内容 & 怎么解决

| 模块 | 内容说明 | 技术实现 |
|------|----------|---------|
| **意图识别** | 判断输入属于 chat / emotional_share / task_request | LLM 一次分类，输出 intent + confidence |
| **场景识别** | 结合屏幕场景 + 时间 + 用户状态理解当前处境 | `pkg/scene` 状态注入 Prompt |
| **任务分类** | 具体任务类型（文件整理/提醒/搜索/邮件等）| LLM 分类，输出 task_type |
| **风险判断** | low/medium/high 三级，决定是否需要确认 | 本地规则表查询 `pkg/risk` |
| **Task 规划** | 复杂任务拆解为多步执行计划 | PicoClaw agent tool_iterations 机制 |
| **陪伴/执行/混合路由** | 根据意图选择走情感对话路径还是任务执行路径 | Go 路由逻辑，分叉 agent 处理流 |
| **主动事件触发决策** | 决定何时、以何种方式主动开口 | `pkg/proactive` 规则引擎 |
| **Prompt 构建器** | 将人设 + 用户状态 + 记忆 + 触发原因组装成完整 Prompt | Go 层拼装，每次 LLM 调用前执行 |
| **情感化汇报生成** | 任务完成后将结果转化为角色口吻的话语 | 二次 LLM 调用，注入角色人设 + 结果摘要 |

### 与 PicoClaw 的关系

| 内容 | 策略 | 原因 |
|------|------|------|
| Agent 主循环 | **复用 PicoClaw** | `pkg/agent/loop.go` 直接使用 |
| Task 规划（多步工具调用）| **复用 PicoClaw** | `max_tool_iterations` 机制已有 |
| 模型路由 | **复用 PicoClaw** | `pkg/routing` + `pkg/providers` 支持 Claude/DeepSeek/Qwen |
| 意图识别 | **改造 PicoClaw** | PicoClaw agent 有工具选择逻辑，扩展加情绪分类分支 |
| Prompt 构建器 | **改造 PicoClaw** | PicoClaw 有基础 context 拼装，扩展加记忆注入 + 用户状态注入 |
| 情感化汇报生成 | **改造 PicoClaw** | 在 agent 返回后加一层 LLM 处理包装角色口吻 |
| 陪伴/执行路由 | **改造 PicoClaw** | 在 agent 主循环增加路由分叉逻辑 |
| 风险判断 | **GoClaw 新建** | PicoClaw 无权限风险概念，新建 `pkg/risk` |
| 主动事件触发决策 | **GoClaw 新建** | 新建 `pkg/proactive`（基于改造的 heartbeat）|

---

## 第五层：执行能力层（系统辅助能力核心）

### 这一层解决什么问题

它怎么真的替用户动手。

这是桌宠区别于所有纯聊天产品的核心能力。没有这一层，桌宠只是一个有表情的聊天框；有了这一层，用户才能真实体验到「他帮我做了一件事」。任务执行的全程都需要有角色的情感包装——用户感受到的应该是「他在帮我」而不是「工具在运行」。

### 包含什么内容 & 怎么解决

| 模块 | 内容说明 | 技术实现 |
|------|----------|---------|
| **Execution Task 管理** | 任务状态机（pending/running/success/failed），进度实时回传 | Go 任务队列 + WebSocket 进度推送 |
| **文件操作能力** | 整理、重命名、创建文件夹、移动、读取 | PicoClaw `pkg/tools`（read_file/write_file/list_dir）|
| **网页搜索能力** | 信息查询，返回摘要 + 来源 | PicoClaw `pkg/tools/web`（DuckDuckGo/Brave/Tavily）|
| **定时提醒** | 设置、管理、到时自动触发 | PicoClaw `pkg/cron` |
| **浏览器操作能力** | 打开指定网页、填写表单 | PicoClaw exec 工具（V2）|
| **进程干预能力** | 监控耗时脚本，完成后主动通知 | PicoClaw exec + 进度回调扩展（V2）|
| **系统 API 调用** | 发邮件、系统通知、日历管理 | 新建专项工具（V3）|
| **沙箱执行** | 高风险操作隔离 | PicoClaw `restrict_to_workspace` + 危险命令拦截 |
| **快照 & 撤销** | 中高风险操作前自动保存快照，10 分钟内可撤销 | 新建 `pkg/snapshot` |

### 与 PicoClaw 的关系

| 内容 | 策略 | 原因 |
|------|------|------|
| 文件操作（read/write/list）| **复用 PicoClaw** | `pkg/tools` 已有完整文件操作 |
| 网页搜索 | **复用 PicoClaw** | `pkg/tools/web` 已有多搜索引擎支持 |
| 定时提醒 | **复用 PicoClaw** | `pkg/cron` 已有完整定时任务 |
| exec 命令执行 | **复用 PicoClaw** | `pkg/tools` exec 工具直接用 |
| 沙箱隔离 | **复用 PicoClaw** | `restrict_to_workspace` + 危险命令黑名单已内置 |
| Execution Task 管理 | **改造 PicoClaw** | 在 agent tool 调用基础上加进度回传 + 状态管理 |
| 浏览器操作 / 进程监控 | **改造 PicoClaw** | V2，基于 exec 扩展加回调机制 |
| 快照 & 撤销 | **GoClaw 新建** | PicoClaw 无此概念，新建 `pkg/snapshot` |
| 系统 API（邮件/日历） | **GoClaw 新建** | V3，新建专项工具 |

---

## 第六层：安全与沉淀层（底座）

### 这一层解决什么问题

为什么用户敢用，以及为什么越用越有价值。

用户愿意让桌宠动他的文件、读他的屏幕、存他说过的话，前提是「我知道它在做什么、我能控制它、出了问题能恢复」。这一层如果缺失或设计粗糙，整个产品的可信度就崩塌了。

### 包含什么内容 & 怎么解决

| 模块 | 内容说明 | 技术实现 |
|------|----------|---------|
| **权限策略** | 三级权限（low自动/medium确认/high授权），逐层收紧 | `pkg/risk` 规则表，前端对应三种确认 UI |
| **风险分级** | 按操作类型 + 操作范围综合判定风险等级 | `pkg/risk` 规则引擎 |
| **白名单机制** | 用户可将常用操作加入白名单跳过每次确认 | SQLite 配置表（V2）|
| **双重确认** | 高风险操作必须用户明确授权，禁止倒计时自动确认 | 前端强制交互确认组件 |
| **审计日志** | 所有 OpenClaw 执行记录，可查看、可溯源 | SQLite 操作日志表 |
| **任务历史** | 最近 50 条任务记录，支持单条撤销（有效期内）| SQLite + 快照机制 |
| **关系沉淀** | 亲密度、记忆、对话历史长期积累，卸载不丢失 | 加密 SQLite 本地存储 |
| **用户偏好持久化** | 权限设置、提醒偏好、静默时段等持久化 | SQLite 配置表 |
| **误触发静默机制** | 1 小时内触发 > 2 次自动进入静默期；连续 3 次忽略当日停止 | `pkg/proactive` 冷却管理 |
| **崩溃自恢复** | OpenClaw 子进程崩溃自动回滚并重启 | Go supervisor 模式 + 快照回滚 |
| **数据清除** | 用户可随时清除全部记忆和历史 | SQLite DELETE + 文件清理 |

### 与 PicoClaw 的关系

| 内容 | 策略 | 原因 |
|------|------|------|
| 沙箱隔离 | **复用 PicoClaw** | `restrict_to_workspace` 直接用 |
| 危险命令拦截 | **复用 PicoClaw** | exec 安全守卫（rm -rf 等）已内置 |
| 权限分级 | **GoClaw 新建** | PicoClaw 无此概念，新建 `pkg/risk` |
| 风险分级规则引擎 | **GoClaw 新建** | 归入 `pkg/risk` |
| 操作日志（基础）| **改造 PicoClaw** | PicoClaw 有内部日志，扩展为用户可见的执行记录 |
| 本地加密存储 | **改造 PicoClaw** | PicoClaw 用 Markdown 文件，改为加密 SQLite |
| 快照 & 撤销 | **GoClaw 新建** | 与第五层共用 `pkg/snapshot` |
| 白名单机制 | **GoClaw 新建** | V2，新建配置表 |
| 误触发静默机制 | **GoClaw 新建** | 归入 `pkg/proactive` 的冷却管理 |
| 崩溃自恢复 | **改造 PicoClaw** | PicoClaw 有进程管理，扩展加回滚 + 重启逻辑 |
| 隐私授权 UI | **GoClaw 新建** | 首次启动授权流程，Electron 前端实现 |

---

## 需要新建和改造的模块汇总

### 新建模块（GoClaw 独有）

| 模块 | 路径 | 所属层 | 说明 |
|------|------|--------|------|
| Desktop 频道 | `pkg/channels/desktop` | 交互承接层 | WebSocket 服务，替代 Telegram 成为本地通信总线 |
| 场景感知 | `pkg/scene` | 交互承接层 | 屏幕识别 + 键盘频率 + 时段判断 |
| 情绪状态机 | `pkg/emotion` | 陪伴与关系层 | 心情值/精力值/亲密度，驱动 Live2D 表情 |
| 主动触发引擎 | `pkg/proactive` | 陪伴与关系层 + 决策层 | 规则引擎 + 冷却管理 + 静默机制 |
| 风险评估 | `pkg/risk` | 决策层 + 安全层 | 三级权限规则表 + 白名单 + 确认流程 |
| 快照与撤销 | `pkg/snapshot` | 执行层 + 安全层 | 操作前快照 + 10 分钟撤销窗口 |
| Electron 前端 | `frontend/` | 表现层 | Live2D + 气泡 UI + 设置面板 + 授权流程 |

### 改造模块（在 PicoClaw 基础上扩展）

| 模块 | PicoClaw 原路径 | 扩展内容 |
|------|----------------|---------|
| 记忆系统 | `pkg/memory` | 加 SQLite 结构化存储、重要性评分、过期时间、recall 计数 |
| 周期任务 | `pkg/heartbeat` | 改造为场景感知规则引擎，加冷却机制 |
| 定时任务 | `pkg/cron` | 扩展加时段判断、纪念日追踪、日常节奏节点 |
| Agent 主循环 | `pkg/agent` | 加意图分类分叉、情感路由、Prompt 注入记忆和用户状态 |
| 操作日志 | `pkg/logger` | 扩展为用户可见的审计日志，存入 SQLite |
| 本地存储 | `workspace/` | 从 Markdown 文件迁移到加密 SQLite |
| 进程管理 | `cmd/picoclaw` | 扩展加崩溃自恢复 + 快照回滚逻辑 |

---

## 优先级与里程碑

> 优先级说明：**P0 = 阻断性，没有它后面的任务无法开始；P1 = 核心功能，演示必须有；P2 = 体验增强，有了更好；P3 = 锦上添花，时间允许再做**

---

### **V1（M1）：跑通核心闭环，能对外演示**

**目标**：用户能看到角色、能对话、能让角色帮忙做一件事、角色能主动说一句话。

**验收标准**：演示者说「帮我整理桌面」，角色动起来、执行任务、用角色口吻汇报结果。整条链路无断点。

| 优先级 | 层次 | 任务 | 策略 | 说明 |
|--------|------|------|------|------|
| **P0** | 交互承接层 | **Desktop WebSocket 频道** | 新建 `pkg/channels/desktop` | 前后端通信总线，所有功能的基础，第一个做 |
| **P0** | 表现层 | **Electron 透明桌面窗口（可运行）** | 新建 | 没有窗口，一切无从演示 |
| **P0** | 决策与编排层 | **接通 PicoClaw Agent 主循环** | 复用 PicoClaw | Desktop 频道接通 agent，文字进、文字出，先跑通 |
| **P0** | 陪伴与关系层 | **角色人设加载（SOUL.md）** | 复用 PicoClaw | agent 有人设才能"像个角色"，否则只是裸 LLM |
| **P1** | 表现层 | **Live2D 基础渲染（静态 + 2 种表情）** | 新建 | V1 先做 neutral/happy 两种够演示，不要卡在 4 种表情上 |
| **P1** | 表现层 | **对话气泡 UI** | 新建 | 角色说的话要显示出来，没有气泡演示没有说服力 |
| **P1** | 执行能力层 | **文件整理任务（含进度回传）** | 复用 PicoClaw + 改造 | V1 核心演示场景，必须做好 |
| **P1** | 安全沉淀层 | **中风险任务确认流程** | 新建 `pkg/risk` | 文件整理属于中风险，没有确认直接执行不安全 |
| **P1** | 表现层 | **任务进度气泡** | 新建 | 执行中有反馈才有「他在帮我」的感觉 |
| **P1** | 表现层 | **任务确认弹窗** | 新建 | 配合中风险确认流程 |
| **P1** | 决策与编排层 | **意图识别（三分类）** | 改造 PicoClaw | 区分聊天/情绪/任务，路由到不同处理路径 |
| **P1** | 决策与编排层 | **Prompt 构建器基础版** | 改造 PicoClaw | 注入人设，保证角色语气一致 |
| **P1** | 陪伴与关系层 | **主动触发基础版（深夜超时 1 条规则）** | 改造 heartbeat | V1 只要能演示「角色主动说了一句话」就达标 |
| **P1** | 交互承接层 | **屏幕场景感知（3 种场景）** | 新建 `pkg/scene` | 主动触发需要场景作为判断依据 |
| **P2** | 执行能力层 | **提醒设置任务** | 复用 PicoClaw `pkg/cron` | 演示加分项，不是核心 |
| **P2** | 执行能力层 | **网页信息查询任务** | 复用 PicoClaw | 演示加分项 |
| **P2** | 执行能力层 | **快照 & 撤销基础版** | 新建 `pkg/snapshot` | 安全兜底，V1 建议做，但不影响演示 |
| **P2** | 陪伴与关系层 | **基础记忆（记住用户名字）** | 复用 PicoClaw | 加分项，让角色叫得出名字 |
| **P2** | 安全沉淀层 | **操作日志基础版** | 改造 PicoClaw | 安全性佐证，演示时可展示 |
| **P2** | 交互承接层 | **点击唤醒角色** | 新建 | 交互细节，有了更好 |
| **P3** | 表现层 | **悬浮面板右键菜单** | 新建 | 可先用快捷键替代 |
| **P3** | 安全沉淀层 | **沙箱复用配置** | 复用 PicoClaw | 直接复用不需要开发，但要配置到位 |

**V1 关键路径（必须按序完成）**：
```
P0: WebSocket 频道 → Electron 窗口 → 接通 Agent → 人设加载
    ↓
P1: Live2D 基础渲染 → 对话气泡 → 意图识别 → 文件整理任务
    ↓
P1: pkg/risk 确认流程 → 任务进度气泡 → 场景感知 → 主动触发
```

---

### **V2（M2）：情感系统完整，体验真实**

**目标**：用户使用 3 天后说「它记得我」，使用 1 周后说「我们越来越熟了」。

**验收标准**：用户告诉角色一件重要的事，3 天后角色在对话中自然提起；角色能通过语音说话；亲密度变化用户可感知。

| 优先级 | 层次 | 任务 | 策略 | 说明 |
|--------|------|------|------|------|
| **P0** | 陪伴与关系层 | **记忆结构化扩展（SQLite + 重要性评分 + 过期）** | 改造 PicoClaw `pkg/memory` | V2 的核心，所有「他记得我」体验的基础 |
| **P0** | 决策与编排层 | **Prompt 构建器完整版（注入记忆 + 用户状态）** | 改造 PicoClaw | 记忆做了但不注入没用，两者必须配套 |
| **P1** | 陪伴与关系层 | **亲密度系统完整版（四阶段）** | 新建 `pkg/emotion` | V2 最重要的情感差异化功能 |
| **P1** | 陪伴与关系层 | **情绪状态机（心情值/精力值）** | 新建 `pkg/emotion` | 驱动 Live2D 表情多样化，让角色「活」起来 |
| **P1** | 表现层 | **更多动作动画（工作/邀功/撒娇）** | 新建 | 情绪状态机做了，表情层要跟上 |
| **P1** | 陪伴与关系层 | **主动触发完整版（5 条规则 + 冷却管理）** | 改造 → `pkg/proactive` | V1 只有 1 条规则，V2 要让主动触发真正有价值 |
| **P1** | 安全沉淀层 | **误触发静默机制** | 新建（归入 `pkg/proactive`）| 主动触发完整版配套，防止过度打扰 |
| **P1** | 决策与编排层 | **情感化汇报生成** | 改造 PicoClaw | 任务完成后角色用自己的语气说话，而不是系统提示 |
| **P1** | 交互承接层 | **语音输入 ASR** | 复用 PicoClaw（Groq Whisper）| 语音交互是 V2 的体验升级核心 |
| **P1** | 表现层 | **TTS 语音输出** | 新建（Fish Speech）| 有语音输入就要有语音输出，否则交互不完整 |
| **P2** | 交互承接层 | **唤醒词检测** | 新建（Porcupine）| 配合语音输入，不叫名字也能触发 |
| **P2** | 交互承接层 | **键盘活跃度感知精确版** | 新建 | 让专注度判断更准确，减少误触发 |
| **P2** | 交互承接层 | **紧急制动快捷键** | 新建 | 安全兜底，V2 建议做 |
| **P2** | 陪伴与关系层 | **日常节奏管理（每日/每周节点）** | 改造 `pkg/cron` | 周一打招呼、周五收尾提醒，增加仪式感 |
| **P2** | 决策与编排层 | **场景识别完整版（5 种场景）** | 改造 `pkg/scene` | V1 是 3 种，V2 扩展到 5 种 |
| **P2** | 表现层 | **亲密度光圈动效** | 新建 | 亲密度变化的视觉反馈，加分项 |
| **P2** | 安全沉淀层 | **本地加密 SQLite** | 改造 PicoClaw | 记忆扩展了，存储安全要跟上 |
| **P2** | 安全沉淀层 | **隐私授权 UI（首次启动）** | 新建 | V2 数据更敏感，授权流程要完整 |
| **P3** | 执行能力层 | **浏览器操作（打开网页）** | 改造 PicoClaw | 执行能力扩展，有了更好 |
| **P3** | 执行能力层 | **进程监控（完成后通知）** | 改造 PicoClaw | 执行能力扩展 |
| **P3** | 安全沉淀层 | **白名单机制** | 新建 | 频繁用户的便利性，非核心 |

**V2 关键路径**：
```
P0: 记忆 SQLite 扩展 → Prompt 构建器完整版
    ↓
P1: 亲密度系统 + 情绪状态机 → Live2D 动作扩展
    ↓
P1: 主动触发完整版 + 静默机制
    ↓
P1: 语音输入（ASR）+ 语音输出（TTS）
```

---

### **V3（M3）：交付成果，稳定可展示**

**目标**：能完整演示从「第一次见面」到「深度信任」的用户旅程，答辩时能清晰说明差异化价值。

**验收标准**：演示者从零开始使用桌宠，5 分钟内能感受到三个差异化亮点：「它记得我说过的事」「它主动关心了我」「它帮我真正做了一件事」。

| 优先级 | 层次 | 任务 | 策略 | 说明 |
|--------|------|------|------|------|
| **P0** | 全局 | **V1 + V2 遗留 Bug 修复与稳定性** | — | 答辩演示不能出故障，稳定性第一 |
| **P0** | 表现层 | **表情与动画打磨（流畅度、过渡）** | 迭代 | 演示时视觉体验直接影响评审印象 |
| **P0** | 安全沉淀层 | **崩溃自恢复 + 快照回滚** | 改造 PicoClaw | 演示环境下任务失败必须能优雅恢复 |
| **P1** | 陪伴与关系层 | **亲密度阶段解锁内容（话语/称呼变化）** | 新建 | 答辩核心亮点之一：「关系真的在变」 |
| **P1** | 陪伴与关系层 | **纪念日追踪（生日/初次对话日）** | 改造 `pkg/cron` | 演示亮点：「它记住了我们的第一次对话」 |
| **P1** | 表现层 | **设置面板完整版（记忆管理/权限/日志）** | 新建 + 改造 | 答辩时展示「透明可控」是信任建立的关键 |
| **P1** | 安全沉淀层 | **数据清除功能** | 新建 | 和设置面板配套，隐私保护的完整闭环 |
| **P1** | 安全沉淀层 | **任务历史完整版（可查看 + 单条撤销）** | 改造 | 演示时展示操作记录，增加可信度 |
| **P2** | 陪伴与关系层 | **叙事剧情库基础版** | 新建 | 答辩加分项：角色有自己的故事 |
| **P2** | 陪伴与关系层 | **每月节奏管理** | 改造 `pkg/cron` | 长期陪伴感的体现 |
| **P2** | 决策与编排层 | **碎片化叙事触发规则** | 新建 | 配合剧情库，角色在日常对话中透露背景 |
| **P2** | 表现层 | **剧情 / 图鉴界面** | 新建 | 亲密度解锁内容的展示界面 |
| **P2** | 执行能力层 | **邮件起草与发送** | 新建 | 执行能力扩展，演示多样性 |
| **P2** | 安全沉淀层 | **整体安全审计** | — | 答辩时能说清楚「哪些数据在本地」 |
| **P3** | 交互承接层 | **拖拽文件到角色触发任务** | 新建 | 交互细节，时间允许再做 |
| **P3** | 执行能力层 | **系统通知 API** | 新建 | 提醒体验优化，非核心 |

**V3 关键路径**：
```
P0: 稳定性修复 + 动画打磨 + 崩溃自恢复
    ↓
P1: 亲密度解锁内容 + 纪念日追踪（演示核心叙事）
    ↓
P1: 设置面板完整版 + 数据清除 + 任务历史（演示可信度）
```

---

**一句话总结各版本重心**：

- **V1**：先让链路跑通，从 WebSocket 到 Agent 到任务执行，有角色、能说话、能干活
- **V2**：让情感系统活起来，记忆、亲密度、情绪、语音，用户用了几天之后感受到关系在积累
- **V3**：打磨演示质量，补齐叙事内容，保证答辩时的稳定性和说服力

---

## 快速参考：新建模块工作量估算

| 模块 | 预估代码量 | 开发重点 | 里程碑 |
|------|-----------|---------|--------|
| `pkg/channels/desktop` | ~400 行 | WebSocket 事件协议设计 | **V1** |
| `pkg/scene` | ~300 行 | 系统 API 调用 + 场景规则 | **V1** |
| `pkg/risk` | ~200 行 | 规则表 + 前端确认流程 | **V1** |
| `pkg/snapshot` | ~200 行 | 快照写入 + 撤销逻辑 | **V1** |
| `pkg/emotion` | ~300 行 | 状态机逻辑 + WebSocket 推送 | **V2** |
| `pkg/proactive` | ~300 行 | 规则引擎 + 冷却管理 | **V2** |
| `frontend/` (Electron) | 大（~3000 行）| Live2D + 气泡 UI + 交互 | **V1-V3 持续** |

---

*文档版本：v1.0 | 日期：2026-03-25*
