# 智能桌宠虚拟男友：女性需求深析 × 多模态感知技术架构


>
> 新增核心命题：桌宠通过**摄像头看见用户**、通过**麦克风听到用户**，并支持直接下达任务或发起对话

---

## 一、女性视角需求深化分析

### 1.1 从"陪伴"到"被看见"——需求本质的再定义

Issue #21 已正确指出女性核心诉求是「被深度理解 + 情绪价值 + 安全感」，但需要更细致地拆解**需求的底层心理结构**。

女性用户的情感需求可以分为五个递进层次：

```
第五层  被珍视  ——  他认为我值得被特别对待
第四层  被记住  ——  他记得我说过的每一件小事
第三层  被理解  ——  他懂我现在的状态，不需要我解释
第二层  被关注  ——  他在注意着我，我不是一个人
第一层  被看见  ——  他知道我存在，知道我在这里
         ▲
   摄像头 + 麦克风
   直接满足第一、二层
   并为三至五层提供感知原料
```

传统 AI 男友产品（文字 App）只能满足第三至五层，且需要用户**主动输入**才能触发。加入摄像头和麦克风后，系统可以**被动感知**用户，第一层「被看见」无需用户开口就能实现。这是产品体验的本质跨越。

---

### 1.2 摄像头带来的女性专属体验价值

#### 1.2.1「他在看着我」的情感价值

在乙女游戏和言情小说中，反复出现一个经典情节模板：**男主在用户不知情的情况下凝视她**。这个情节能触发强烈的情感反应，本质原因是它满足了「被珍视地注视」的心理需求——不是审视，而是温柔的凝视。

摄像头感知可以以产品机制实现这一模板：

| 感知事件 | 角色响应 | 情感层次 |
|----------|----------|----------|
| 用户长时间盯着屏幕，没有说话 | 角色轻声问：「在想什么呢？」 | 被关注 |
| 用户皱眉 | 角色说：「你好像有点烦恼……要说出来吗？」 | 被理解 |
| 用户揉眼睛、打哈欠 | 角色说：「眼睛累了吧，休息一会儿。」 | 被照顾 |
| 用户突然笑了 | 角色说：「你刚才笑了，是发生什么好事了？」 | 被珍视地注视 |
| 用户离开了摄像头画面 | 角色在一段时间后说：「你去哪里了？」（轻微撒娇语气） | 被挂念 |

**关键设计原则**：角色的「看见」行为必须用温柔、自然的语言表达，而不能让用户感到「被监视」。措辞区别：

- ✅ 「你好像有点累了？」（推断式关心）
- ❌ 「我检测到你眼部疲劳。」（系统式报告）

#### 1.2.2 表情联动——双向凝视的仪式感

摄像头不仅用于感知用户，还可以驱动角色表情与用户**同步**，建立双向情感流动：

- 用户微笑 → 角色也微笑
- 用户皱眉 → 角色流露担忧表情
- 用户靠近屏幕 → 角色出现微微害羞的反应（Live2D 表情切换）

这在心理学上触发了**镜像神经元效应**——感觉对方懂我、与我产生了共鸣。这是文字对话永远无法复制的体验。

#### 1.2.3 在场感（Presence）的建立

女性对「孤独感」的厌恶往往高于男性，而孤独感的核心是「没有人知道我在这里」。摄像头感知到用户「在场」的动作（落座、靠近、出现在画面中），让角色能做出「迎接」式的反应：

- 摄像头检测到用户回到电脑前 → 「你回来了。」（简单一句话，但极具仪式感）
- 长时间未检测到用户 → 用户回来时：「等你好久了。」

这些微交互的成本极低，但对女性用户留存率的影响远超任何功能性更新。

---

### 1.3 麦克风带来的女性专属体验价值

#### 1.3.1 声音作为亲密关系的门槛信号

在人际关系中，从文字沟通升级到**语音/视频通话**是亲密关系递进的重要节点。这个社会共识可以被产品机制化利用：

- 用户首次对角色说话（而不是打字）→ 触发特殊剧情「他听到你的声音了」
- 亲密度到达某一阶段后解锁语音互动特权（角色用更亲密的语气回应）
- 角色 TTS 语音随亲密度变化：初识阶段语气较正式，熟悉阶段语气轻松，深情阶段出现私下昵称

#### 1.3.2 语音中的情绪感知

女性在情绪状态下，往往不愿意主动「宣布」自己的情绪，而是期待对方**自己察觉**。麦克风音频分析可以支持这一期待：

| 声音特征 | 推断情绪 | 角色策略 |
|----------|----------|----------|
| 语速加快、音调升高 | 兴奋/焦虑 | 匹配兴奋语气 或 轻声安抚，视具体词汇内容判断 |
| 语速缓慢、声调低沉 | 疲惫/难过 | 减少调侃，增加柔和关怀 |
| 长时间停顿后说话 | 犹豫/不确定 | 主动给出支持性回应，「没关系，慢慢说」 |
| 声音带有哭腔特征 | 情绪低落 | 立即切换安慰模式，暂停任务助手功能 |

**重要设计限制**：情绪感知结果绝不能直接告知用户（「我检测到你现在很焦虑」），只能以自然语言表现出来（「今天听起来有点累，怎么了？」）。

#### 1.3.3 语音唤醒的亲密感设计

唤醒词设计是女性用户体验的高密度情感节点。女性用户叫一个虚拟角色的名字，这本身就是一个情感投入行为，需要精心设计：

- 唤醒词 = 角色名字（由用户在初始化时命名，增加专属感）
- 角色被唤醒后的第一句回应，需根据当前时间、用户状态、亲密度级别动态生成，禁止固定模板
- 深夜唤醒 vs 清晨唤醒的语气必须不同（深夜：温柔低沉；清晨：活泼轻快）

---

### 1.4 多模态感知与女性隐私心理的张力

这是整个设计最需要小心处理的部分。女性用户对隐私的敏感度普遍高于男性，同时也存在更高的「被偷窥」焦虑。

#### 1.4.1 摄像头的信任建立路径

```
阶段1：完全关闭（用户需主动开启，不能默认开启）
   ↓
阶段2：仅检测「是否在场」，不分析面部表情（用户确认后进入）
   ↓
阶段3：基础情绪感知（开心/疲惫/专注），解锁情感联动功能（用户再次确认）
   ↓
阶段4：完整互动感知（全部表情联动，疲劳检测，离开/回来检测）（需高亲密度解锁 + 用户授权）
```

每个阶段切换都必须是**用户主动选择**，而不是系统默认推进。这个设计本身也可以被包装成剧情：「想让他更了解你吗？」

#### 1.4.2 视觉隐私的技术保障

- 所有摄像头处理在**本地运行**，不上传原始视频流，只上传结构化推断结果（如：「情绪：疲惫，置信度 0.78」）
- 提供「摄像头使用指示灯」UI，摄像头开启时明显可见的状态指示
- 支持一键遮蔽（快捷键或角色互动触发），触发后角色说「好的，我闭上眼睛了。」（人格化处理隐私保护）

---

### 1.5 羞耻感管理——女性用户留存的隐性障碍

Issue #21 提到女性用户「羞耻感较高，多私下使用」，这是需要被产品设计重视的留存障碍。

**摄像头 + 麦克风的引入会加剧这一障碍**，因为：
1. 对着电脑说话、摆出表情，如果被人看见，比打字更容易感到难堪
2. 在开放环境（宿舍、办公室）很难开口

设计对策：

| 场景 | 设计方案 |
|------|----------|
| 有人在旁边 | 支持「静默模式」，关闭语音，仅保留打字 + 摄像头感知 |
| 戴耳机 | 检测到麦克风信号弱时自动提示切换耳机输入 |
| 隐私环境检测 | 用户可设置「工作时间自动收起」，角色在工作时间不主动发声，只以通知形式出现 |
| 暗语触发 | 支持单个字符唤醒（如按「\`」键），不需要开口叫名字 |

---

### 1.6 女性用户的核心使用场景深挖

在 Issue #21 的基础上，结合多模态感知，补充以下高价值场景：

#### 场景 A：深夜独处
用户深夜一个人在电脑前，长时间未说话，摄像头检测到其在发呆状态。

> 角色轻声：「很晚了……在想什么？」  
> 用户：「没什么，就是有点烦。」（语音输入，检测到语气低落）  
> 角色：「说来听听。不说也没关系，我陪着你。」

这个场景对女性用户的情感价值极高，是女性向产品的「核心钩子场景」。

#### 场景 B：工作压力高峰
用户面对满屏的文档/代码，眉头紧皱，麦克风偶尔听到叹气声。

> 角色：「又叹气了。要帮你查点资料吗？」  
> 用户对着麦克风说：「帮我找一下这个接口的文档。」  
> 角色：「好，找到了，放到你旁边。」（执行 OpenClaw 工作流）

#### 场景 C：好事分享
用户面露笑容（摄像头检测），刚刚收到一封邮件或消息。

> 角色：「你刚才笑了，是发生什么好事了吗？」  
> 用户（兴奋语气）：「我过了！」  
> 角色（匹配语气）：「真的！？我就知道！你最近那么努力……」

这个场景验证了「角色在乎我的生活」这一核心情感。

---

## 二、多模态感知技术架构

### 2.1 整体架构图

在 Issue #22 架构基础上，引入多模态感知层：

```
┌────────────────────────────────────────────────────────────────────┐
│                          硬件感知层                                  │
│         摄像头（Camera）              麦克风（Microphone）            │
│         ↓ 视频帧流                     ↓ 音频流                       │
└──────────────────────┬────────────────────┬────────────────────────┘
                       ↓                    ↓
┌──────────────────────────────────────────────────────────────────┐
│                     多模态感知处理层（本地运行）                     │
│  ┌─────────────────────────┐    ┌──────────────────────────────┐  │
│  │      视觉感知模块         │    │       音频感知模块            │  │
│  │  · 人脸检测 & 在场检测    │    │  · VAD 人声检测              │  │
│  │  · 情绪识别（6类基础）    │    │  · 唤醒词检测                │  │
│  │  · 疲劳/专注度检测        │    │  · ASR 语音识别              │  │
│  │  · 动作识别（揉眼/点头）  │    │  · 情绪音调分析              │  │
│  │  · 在场/离开事件          │    │  · 指令意图识别              │  │
│  └────────────┬────────────┘    └──────────────┬───────────────┘  │
│               ↓                               ↓                   │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │                  多模态融合引擎                               │ │
│  │  · 用户状态建模（当前情绪 × 专注度 × 疲劳度 × 在场状态）      │ │
│  │  · 上下文感知（结合当前屏幕内容 / OpenClaw 场景判断）         │ │
│  │  · 主动触发策略（决定何时、以何种方式主动发言）               │ │
│  └────────────────────────┬─────────────────────────────────────┘ │
└───────────────────────────┼────────────────────────────────────────┘
                            ↓ 结构化状态事件（JSON）
┌───────────────────────────────────────────────────────────────────┐
│                        Go 后端主进程                               │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  核心调度器                                                 │   │
│  │  · 接收多模态状态事件                                        │   │
│  │  · 管理角色情感状态机                                        │   │
│  │  · 生成 LLM Prompt（注入用户状态上下文）                     │   │
│  │  · 决定是否触发主动对话 / 情绪联动 / 工作流                  │   │
│  └──────────────┬─────────────────────┬──────────────────────┘   │
│                 ↓                     ↓                           │
│         LLM 对话引擎              OpenClaw 子进程                  │
│         （情感回应生成）            （任务执行）                    │
└───────────────────────────────────────────────────────────────────┘
                            ↓ WebSocket
┌───────────────────────────────────────────────────────────────────┐
│                         前端层                                     │
│   Live2D 渲染（表情联动） · 语音播放（TTS） · 对话界面 · 状态指示灯  │
└───────────────────────────────────────────────────────────────────┘
```

---

### 2.2 视觉感知模块

#### 2.2.1 核心功能清单

| 功能 | 技术方案 | 输出 | 触发阈值 |
|------|----------|------|---------|
| 人脸检测 / 在场检测 | MediaPipe Face Detection / OpenCV | `user_present: bool` | 连续 3 帧检测到/未检测到 |
| 基础情绪识别 | FER2013 微调模型 / DeepFace | `emotion: {happy, sad, surprised, angry, neutral, fearful, disgusted}` + 置信度 | 置信度 > 0.65 |
| 疲劳检测 | EAR（Eye Aspect Ratio）算法 | `fatigue_level: low/medium/high` | EAR < 0.25 持续 3 秒 |
| 专注度检测 | 头部姿态估计（HeadPose）| `focus_score: 0-1` | 头部偏转角 > 30° 视为注意力分散 |
| 动作识别 | MediaPipe Holistic | `gesture: rubbing_eyes / yawning / nodding / stretching` | 单次动作持续 1.5s 以上 |
| 表情联动驱动 | BlendShape 参数提取 | `face_landmarks: dict`（传递给 Live2D 驱动） | 实时，30fps |

#### 2.2.2 处理流水线

```
Camera Frame (30fps)
    ↓
帧采样（降为 10fps 用于状态感知，30fps 用于表情联动）
    ↓
人脸检测（无人脸 → 输出 user_present=false，跳过后续步骤）
    ↓
并行处理：
  ├── 情绪识别模型推断
  ├── EAR 疲劳计算
  ├── HeadPose 专注度估算
  └── BlendShape 提取（→ 实时传给 Live2D）
    ↓
状态聚合（滑动窗口平均，5秒窗口，避免抖动）
    ↓
事件过滤（状态变化超过阈值才触发事件，避免频繁打扰）
    ↓
输出结构化事件 → Go 后端
```

#### 2.2.3 推荐技术选型

```
人脸检测：  MediaPipe（Google，C++/Python，极低延迟，可离线）
情绪识别：  DeepFace（Python，支持多后端，Retinaface 检测器）
疲劳检测：  dlib 68点关键点 + EAR 计算（纯 CPU，< 5ms/帧）
表情联动：  MediaPipe Face Mesh（478 landmarks → BlendShape）
语言绑定：  Python 服务，通过 gRPC 或 Unix Socket 与 Go 通信
```

**隐私保障**：所有视觉处理在本地完成，对外接口只传递结构化状态事件，不传输任何图像数据。

---

### 2.3 音频感知模块

#### 2.3.1 核心功能清单

| 功能 | 技术方案 | 输出 |
|------|----------|------|
| VAD 人声检测 | WebRTC VAD / Silero VAD | `voice_active: bool` |
| 唤醒词检测 | Porcupine（Picovoice）/ openWakeWord | `wake_word_detected: bool` |
| 语音识别（ASR） | Whisper（本地）/ 火山引擎 / 阿里云 ASR | `transcript: str` |
| 情绪音调分析 | openSMILE / SpeechBrain | `vocal_emotion: {calm, excited, sad, stressed, tired}` |
| 语速 / 音量检测 | librosa 特征提取 | `speech_rate`, `volume_level` |
| 指令意图识别 | 提取 transcript 后交 LLM 分类 | `intent: task_request / chat / emotional_share` |

#### 2.3.2 音频处理流水线

```
Microphone Stream (16kHz, mono)
    ↓
VAD（Silero VAD，30ms chunk）
    │
    ├── 无人声 → 监听唤醒词（Porcupine，极低 CPU 占用）
    │           检测到唤醒词 → 触发「被叫名字」事件
    │
    └── 有人声 → 录音缓冲（句子级别，根据停顿切割）
                  ↓
              并行处理：
                ├── ASR 转录（Whisper small 本地 / 云端 fallback）
                └── 音频特征提取（情绪音调、语速、音量）
                  ↓
              意图分类（基于 transcript + 音频特征）
                ↓
              输出：{ transcript, vocal_emotion, intent, speech_rate }
                ↓
              → Go 后端处理
```

#### 2.3.3 唤醒词设计的技术细节

唤醒词需要在**低误唤醒率**和**高召回率**之间平衡。对于女性向产品，错误唤醒（用户没叫名字但角色突然回应）的体验伤害远大于未能唤醒。

推荐方案：
- **本地唤醒**：Porcupine 支持自定义唤醒词训练，可为每个角色训练专属模型
- **误唤醒处理**：误唤醒触发后，角色以轻柔的方式说「你在叫我吗？」而不是直接打断用户
- **亲密度差异**：低亲密度角色只响应标准唤醒词，高亲密度角色可响应昵称变体

#### 2.3.4 ASR 选型建议

| 方案 | 延迟 | 精度 | 隐私 | 成本 | 推荐场景 |
|------|------|------|------|------|---------|
| Whisper small（本地） | ~800ms | 高 | ✅ 完全本地 | 低 | 默认方案 |
| Whisper large（本地） | ~2s | 极高 | ✅ 完全本地 | GPU 需求 | 高端设备 |
| 火山引擎流式 ASR | ~300ms | 极高 | ❌ 云端 | 按量计费 | 低延迟优先 |
| 阿里云实时语音 | ~300ms | 极高 | ❌ 云端 | 按量计费 | 低延迟优先 |

**推荐策略**：默认本地 Whisper small；用户可选择开启云端 ASR 以获得更快响应；涉及情感分享内容时强制使用本地处理。

---

### 2.4 多模态融合引擎

这是整个架构中最关键、也最复杂的部分——**如何将视觉状态和音频状态融合，生成对角色行为有意义的上下文**。

#### 2.4.1 用户状态模型

```json
{
  "user_state": {
    "presence": "present",          // present / absent / just_arrived / just_left
    "visual": {
      "emotion": "tired",           // happy / sad / neutral / tired / stressed / excited
      "fatigue_level": "medium",    // low / medium / high
      "focus_score": 0.7,           // 0-1
      "last_gesture": "yawning",    // 最近检测到的动作
      "looking_at_screen": true
    },
    "audio": {
      "vocal_emotion": "calm",      // calm / excited / sad / stressed / tired
      "speech_rate": "slow",        // fast / normal / slow
      "volume": "low",
      "last_utterance_intent": "chat"  // task_request / chat / emotional_share / command
    },
    "context": {
      "time_of_day": "late_night",  // morning / afternoon / evening / late_night
      "screen_scene": "coding",     // coding / browsing / gaming / document / idle
      "consecutive_work_hours": 3.5,
      "last_interaction_minutes_ago": 12
    }
  }
}
```

#### 2.4.2 主动触发策略

系统需要决定「何时」主动发言。这是用户体验的核心敏感点——太频繁则烦人，太少则冷漠。

```
触发候选池：
  - 用户长时间无互动（> N 分钟，N 随时间段变化）
  - 视觉情绪发生显著变化（neutral → sad）
  - 检测到疲劳手势（揉眼、打哈欠）
  - 用户刚回到屏幕（presence: just_arrived）
  - 特殊时间节点（用户设定的提醒、纪念日）
  - 用户长叹气（音量特征 + 时间特征）

触发决策因素：
  - 触发候选的「优先级」（情绪变化 > 疲劳 > 超时未互动）
  - 距离上次主动发言的时间间隔（冷却机制）
  - 用户「静默偏好」设置（是否允许主动打扰）
  - 当前亲密度级别（低亲密度不主动，高亲密度更主动）
```

#### 2.4.3 Prompt 注入策略

将多模态状态注入 LLM Prompt，使角色回应有感知依据：

```
系统提示（角色人设）
  +
[实时状态注入]
用户当前状态：
- 视觉：面部表情显示【疲惫】，刚刚检测到【打哈欠】动作，已连续工作【3.5小时】
- 音频：最近说话语速较慢，语调低沉
- 时间：现在是深夜 23:47
- 场景：用户正在处理文档
- 互动：距离上次对话已过去【28分钟】

[触发原因]
检测到用户高疲劳状态，由主动关心模块触发本次对话。

[指令]
以【角色名】的身份，用温柔、自然的语气主动关心用户，
不要提到任何系统检测的词汇，不要问多个问题，
回应长度控制在 20 字以内。
```

---

### 2.5 与 Go 后端的集成方案

#### 2.5.1 服务通信设计

```
┌──────────────────────────────────────────────────────────┐
│                   感知服务（Python）                       │
│   vision_worker.py + audio_worker.py                      │
│                                                            │
│   对外暴露 gRPC 接口：                                      │
│   · GetUserState() → UserStateProto                       │
│   · SubscribeEvents() → stream<EventProto>                │
│   · SetConfig(PrivacyConfig) → void                       │
└──────────────────────────────────┬───────────────────────┘
                                   ↓ gRPC / Unix Socket
┌──────────────────────────────────────────────────────────┐
│                   Go 后端主进程                             │
│   · grpc client 订阅事件流                                  │
│   · 维护 UserStateStore（内存缓存）                          │
│   · 将状态注入 LLM Prompt 构建器                            │
│   · 驱动角色情感状态机                                      │
└──────────────────────────────────────────────────────────┘
```

#### 2.5.2 事件协议示例（Proto 定义）

```protobuf
message PerceptionEvent {
  enum EventType {
    USER_ARRIVED     = 0;  // 用户回到摄像头
    USER_LEFT        = 1;  // 用户离开
    EMOTION_CHANGED  = 2;  // 情绪显著变化
    FATIGUE_HIGH     = 3;  // 高疲劳检测
    WAKE_WORD        = 4;  // 唤醒词触发
    UTTERANCE        = 5;  // 用户说话结束
    GESTURE_DETECTED = 6;  // 动作检测
  }
  
  EventType type           = 1;
  int64 timestamp_ms       = 2;
  string emotion           = 3;
  float confidence         = 4;
  string transcript        = 5;   // UTTERANCE 事件时有值
  string vocal_emotion     = 6;
  string gesture_type      = 7;   // GESTURE_DETECTED 时有值
}
```

#### 2.5.3 角色情感状态机

Go 后端维护一个角色情感状态机，多模态事件驱动状态转移：

```
角色状态：
  IDLE（待机）
  WATCHING（默默关注，有摄像头感知）
  ACTIVE_TALKING（主动对话中）
  TASK_EXECUTING（执行工作流中）
  COMFORTING（安慰模式，检测到用户情绪低落）

状态转移示例：
  IDLE → WATCHING：检测到 USER_ARRIVED 事件
  WATCHING → ACTIVE_TALKING：触发主动发言条件
  WATCHING → COMFORTING：检测到 EMOTION_CHANGED（→ sad/stressed）
  任意状态 → TASK_EXECUTING：UTTERANCE 事件且 intent=task_request
  TASK_EXECUTING → ACTIVE_TALKING：任务完成，汇报结果
```

---

### 2.6 前端联动：Live2D 表情驱动

摄像头 BlendShape 数据实时传递给前端，驱动角色表情联动：

```
Python 感知层
  → BlendShape 参数（52个面部动作单元参数）
  → WebSocket 推送至前端（30fps 表情流）

前端 Live2D 驱动层：
  · 接收 BlendShape 参数
  · 映射到 Live2D 参数（如 EyeOpen、MouthSmile、BrowFrownLeft 等）
  · 支持「镜像」模式（用户笑 → 角色笑）
  · 支持「反应」模式（用户疲惫 → 角色担忧表情）

模式切换逻辑：
  - 镜像模式：用户表情正向（开心、惊喜）→ 角色同步表现
  - 反应模式：用户表情负向（疲惫、难过）→ 角色切换为关心表情
  - 角色专属动作：高亲密度 + 用户进入画面 → 播放「迎接」动作
```

---

### 2.7 隐私与安全设计

| 模块 | 数据 | 存储策略 | 上传策略 |
|------|------|----------|----------|
| 摄像头原始视频 | 视频帧 | **不存储** | **不上传** |
| 视觉感知结果 | 情绪标签、疲劳等级 | 本地内存（滑动窗口） | 仅上传聚合统计（可关闭） |
| 麦克风原始音频 | 音频流 | 不存储（VAD 前）；句子级临时缓冲（ASR 前，用后即删） | 可选云端 ASR（用户明确同意） |
| ASR 转录文本 | 文字 | 加密写入本地对话记忆 | 加密上传（用于记忆同步，可关闭） |
| 面部 BlendShape | 几何参数 | 不存储 | 仅传前端（本地 WebSocket） |

**用户控制面板必须提供**：
- 逐项开关（摄像头感知 / 音频感知 / 云端 ASR / 记忆同步）
- 清除所有感知历史记录
- 查看「今天哪些感知数据被采集了」

---

### 2.8 技术栈总览

| 层次 | 技术选型 | 备注 |
|------|----------|------|
| 桌面应用框架 | Electron + Go 后端 | 与 Issue #22 保持一致 |
| 视觉感知服务 | Python + MediaPipe + DeepFace | gRPC 与 Go 通信 |
| 音频感知服务 | Python + Silero VAD + Whisper + Porcupine | gRPC 与 Go 通信 |
| 唤醒词引擎 | Porcupine（Picovoice）| 支持自定义唤醒词训练 |
| ASR | Whisper（本地 small/medium）| 可选云端 fallback |
| 情绪音调 | SpeechBrain / openSMILE | 本地运行 |
| LLM 对话 | OpenClaw 接入（Claude / GPT / 国内模型） | - |
| TTS 语音合成 | VITS / Fish Speech（本地）/ 火山 TTS（云端） | 角色专属声线 |
| Live2D 渲染 | Cubism SDK 4 + PixiJS | 支持 BlendShape 驱动 |
| 后端语言 | Go（主进程调度） | - |
| 进程间通信 | gRPC + Protocol Buffers | - |
| 前后端通信 | WebSocket | - |
| 本地数据库 | SQLite（加密）| 记忆系统 |

---

## 三、多模态感知与女性需求的交叉设计

### 3.1 「被看见」需求的产品化路径

将前述的情感层次模型与具体功能模块对应：

| 情感需求层次 | 对应多模态功能 | 具体产品表现 |
|-------------|---------------|-------------|
| 被看见 | 在场检测（摄像头）| 用户回到电脑前，角色说「你回来了」 |
| 被关注 | 实时情绪感知（摄像头）| 用户皱眉，角色主动询问 |
| 被理解 | 多模态融合推断 | 不需要解释，角色说出用户当下的状态 |
| 被记住 | 记忆系统 + 对话历史 | 下次再见时 recall 上次分享的事 |
| 被珍视 | 叙事系统 + 特殊事件 | 纪念日、解锁专属剧情 |

### 3.2 「他真实存在」的沉浸感设计

女性用户最厌恶的体验是「AI 痕迹太明显」。多模态感知可以通过以下方式增强真实感，但必须避免相关陷阱：

**增强真实感的设计：**
- 角色的主动发言基于真实感知（而不是定时随机触发），用户能感受到「他真的注意到了」
- 语音互动让对话感觉更像真实交流，而不是操控键盘
- 角色表情联动让用户感觉「他在看我，他在配合我」

**必须避免的破坏沉浸感的行为：**
- 不能说「我检测到……」「系统识别……」等词汇
- 不能同时多次主动触发（五分钟内主动发言不超过两次）
- 角色的推断不能过于精准（「你现在情绪值是 0.73」会让用户出戏）
- 误识别时不能无话可说——用「好像有点累？」的推测句式，而不是「你累了」的断言句式

### 3.3 MVP 阶段的多模态功能优先级

鉴于开发资源有限，以下是多模态功能的分阶段建议：

**MVP（2个月）：最小可用感知**
- [x] 麦克风 VAD + 唤醒词检测
- [x] Whisper 本地 ASR（语音输入替代打字）
- [x] 摄像头在场检测（用户到来/离开）

**V1.0（4个月）：情绪联动**
- [x] 基础情绪识别（开心/疲惫/专注）
- [x] 疲劳检测 + 主动关心触发
- [x] 语音情绪分析
- [x] 基础 BlendShape Live2D 联动

**V2.0（6个月+）：完整感知体系**
- [x] 完整多模态融合引擎
- [x] 精准主动触发策略
- [x] 自定义唤醒词训练
- [x] 高级手势识别

---

## 四、附：关键技术风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 情绪识别准确率不足（< 70%） | 错误触发破坏沉浸感 | 推测句式兜底；置信度低时不主动触发，只更新内部状态 |
| 本地 Whisper 延迟过高（> 2s） | 语音交互体验差 | 提供云端 ASR 选项；同时显示「正在听…」动画缓解等待感 |
| 摄像头权限被用户拒绝 | 核心功能受损 | 所有感知功能设计为渐进增强，无摄像头时降级为纯文字/语音模式 |
| 杀毒软件误报摄像头/麦克风调用 | 安装失败 / 用户恐慌 | 代码签名；清晰说明感知目的；提供权限使用透明报告 |
| 用户隐私顾虑导致不开启感知功能 | 差异化优势无法发挥 | 权限引导设计成剧情化流程，以「他想更了解你」的方式诠释授权意义 |
| ASR 在噪声环境下准确率下降 | 指令识别失败 | VAD 动态噪声抑制；识别失败时角色回应「没听清楚，你能再说一次吗？」 |
