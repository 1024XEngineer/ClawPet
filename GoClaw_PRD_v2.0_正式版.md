# GoClaw 智能桌宠 产品需求文档

---

## 文档头

| 项目 | 内容 |
|------|------|
| **文档版本** | v2.0 |
| **作者** | 刘涵月 |
| **评审人** | 待定 |
| **文档状态** | 草案（Draft） |
| **最后更新** | 2026-03-25 |

### 变更记录

| 版本 | 日期 | 修改人 | 变更内容 |
|------|------|--------|----------|
| v1.0 | 2026-03-24 | 刘涵月 | 初稿，完成核心痛点、用户故事、基本概念 |
| v2.0 | 2026-03-25 | 刘涵月 | 按助教建议重构：补充优先级与里程碑、失败定义、风险合规、验收标准；移除过早的数据结构表 |

---

## 一、背景与目标

### 1.1 问题

当前市场上存在两类割裂的产品：

**A 类：有能力，没情感（如 OpenClaw）**
- 能执行文件操作、浏览器自动化、跨应用协同
- 纯指令交互，冷冰冰，用户感受到的是「在用工具」，没有陪伴感

**B 类：有情感，没能力（如桌宠软件、星黎、Replika App）**
- 能提供情绪陪伴、角色扮演
- 只能聊天，不能帮用户做任何真实的事
- 全部为手机 App，PC 端没有常驻陪伴产品

**用户的真实处境**：一个人对着电脑深夜工作，既需要有个角色陪着说「再撑一会儿」，也需要有人帮忙搜资料、整理文件、发提醒——没有任何一款产品能同时做到这两件事。

### 1.2 目标

**产品目标**：构建一个常驻桌面的智能桌宠，让 OpenClaw 的工具执行能力具备情感温度，让情感陪伴能够真正完成任务。

**V1 目标（MVP）**：跑通「感知用户状态 → 主动触发对话 → 接受任务指令 → 执行并反馈」的核心闭环，让一个真实用户在使用 30 分钟后感受到「这不是在用工具，是在和一个真实的伙伴互动」。

**衡量指标（V1）**：
- 任务执行成功率 ≥ 70%（文件整理、提醒设置、信息查询三类）
- 主动触发误打扰率 ＜ 20%（用户专注时不应被打扰）
- 用户 3 日留存率 ≥ 40%

### 1.3 非目标（本期明确不做）

- 多角色同时激活与角色间互动
- 社区功能（角色上传、下载、分享）
- 手机端 / 跨端同步
- 付费订阅与商业化
- 摄像头人脸识别（情绪感知仅通过行为推断，不采集视频）
- 自定义 Live2D 模型上传（V1 使用内置模型）

---

## 二、用户与场景

### 2.1 目标用户

**主要用户：独居或独自使用电脑的年轻女性（18-28 岁）**
- 长时间独自面对电脑，有陪伴需求
- 有乙女游戏、虚拟角色的消费习惯
- 对 AI 工具有一定接受度，但不喜欢冷冰冰的交互

**次要用户：重度 PC 使用者（22-35 岁，性别不限）**
- 每天使用电脑 6 小时以上
- 有大量重复性任务需要自动化
- 希望有一个「一直在旁边的搭档」而不是每次都切窗口调用工具

### 2.2 核心场景分类

**第一类：情绪价值 × 任务完成（GoClaw 核心差异化场景）**

| # | 场景 | 用户状态 | 期望体验 |
|---|------|----------|----------|
| S1 | 论文 / 报告 DDL 前崩溃 | 深夜、高压、卡壳 | 先被接住情绪，再被帮忙写 |
| S2 | 面试前焦虑准备 | 紧绷、反复刷信息 | 角色主动 recall 昨天的约定，帮忙整理备忘 |
| S3 | 信息轰炸期（换工作/搬家） | 信息过载、人已麻了 | 角色帮列清单，一件一件拆开处理 |
| S4 | 凌晨睡不着 | 无目的浏览、轻度焦虑 | 角色主动开口，顺势帮处理未完成的事 |
| S5 | 工作出了纰漏 | 情绪低落、自责 | 先安抚，再帮起草补救邮件 |
| S6 | 独自准备重要的事 | 兴奋但焦虑 | 角色陪着兴奋，同时帮做攻略 |

**第二类：只需要情绪价值**

| # | 场景 | 期望体验 |
|---|------|----------|
| S7 | 一个人吃饭 | 角色陪着，像对面有个人 |
| S8 | 哭完打开电脑 | 角色只说「我在」，不追问不建议 |
| S9 | 任务完成后空落落 | 角色填补空白，不催新任务 |
| S10 | 出差晚上回酒店 | 角色 recall 知道在出差，像在家等的人 |
| S11 | 委屈不想找人说 | 角色接住情绪，不评判不分析 |
| S12 | 某个只有自己记得的日子 | 角色主动提起，让它不只自己知道 |

**第三类：只需要完成任务**

| # | 场景 | 期望体验 |
|---|------|----------|
| S13 | 批量整理文件 | 说一句话，30 秒搞定 |
| S14 | 快速查资料 | 不切窗口，直接拿到带来源的结果 |
| S15 | 一次性设多个提醒 | 说完角色复述确认，时间到主动触发 |
| S16 | 监控耗时脚本 | 跑完主动告诉我，不用一直盯 |
| S17 | 起草措辞敏感的邮件 | 说出大意，角色起草，用户微调，一键发 |

### 2.3 用户故事（精选关键路径）

**US-01：深夜主动陪伴**

> 作为一个深夜独自写报告的用户，我希望角色能在我卡壳的时候主动开口，这样我不需要先开口说「我需要帮助」，就能感受到有人在关注我。

**验收条件**：
- Given 用户在文档类窗口停留 ＞ 30 分钟且键盘输入频率 ＜ 5 次/分钟，时间为 22:00 后
- When 主动触发条件满足且冷却时间已过
- Then 角色说出符合当前场景的关心语，不包含「系统检测到」等词汇，不超过 20 字

**US-02：任务执行与情感包装**

> 作为一个需要整理桌面的用户，我希望直接对角色说「帮我整理桌面」就能完成，同时看到角色有对应的动作和反馈，感受到「他在帮我」而不是「工具在运行」。

**验收条件**：
- Given 用户说出包含操作意图的指令
- When 角色识别为任务请求
- Then 角色先展示预览（中风险任务），用户确认后执行，执行中有进度气泡，完成后用角色口吻汇报，全程不出现「调用 API」「执行指令」等系统词汇

**US-03：记忆 Recall**

> 作为一个昨天告诉角色「明天有面试」的用户，我希望今天开机角色会主动提起，这样我感受到「他记得我说过的事」。

**验收条件**：
- Given 用户在之前会话中提到了一个有时间属性的事件
- When 该事件对应的时间节点到来，用户开机
- Then 角色在打招呼时自然提及，不以「根据我的记录」开头

---

## 三、范围与优先级

### 3.1 功能优先级总表

| 优先级 | 功能模块 | 核心功能点 | 所属版本 |
|--------|----------|------------|----------|
| P0 | 角色渲染 | Live2D 角色显示，4 种基础表情状态切换，可拖拽 | V1 |
| P0 | 对话交互 | 文字输入 + 角色文字回复，气泡展示 | V1 |
| P0 | 任务执行 | 文件整理、提醒设置、信息查询 3 类任务 | V1 |
| P0 | 任务风险确认 | 中风险任务展示预览确认，高风险要求明确授权 | V1 |
| P0 | 基础记忆 | 记住用户名字，当日对话摘要次日可 recall | V1 |
| P0 | 主动触发（基础） | 深夜工作超时触发，用户完成大任务后的空落感触发 | V1 |
| P0 | 场景感知（基础） | 识别 3 种场景：文档、浏览器、桌面空闲 | V1 |
| P1 | 语音输入 | Whisper 本地 ASR，唤醒词触发 | V1.1 |
| P1 | 长期记忆 | 重要事件永久保留，相似度检索 recall | V1.1 |
| P1 | 亲密度系统 | 4 个阶段，影响角色语气和解锁内容 | V1.1 |
| P1 | 主动触发（完整） | 5 类触发规则，冷却机制，场景专注度过滤 | V1.1 |
| P1 | 场景感知（扩展） | 识别 5 种场景，专注度检测 | V1.1 |
| P1 | TTS 语音输出 | 角色专属声线，任务汇报有语音 | V1.1 |
| P2 | 多角色系统 | 创建、切换多个角色，数据隔离 | V2 |
| P2 | 情绪状态机完整版 | 心情值 / 精力值动态驱动，事件触发状态变化 | V2 |
| P2 | 纪念日追踪 | 生日、初次对话日等特殊日期主动触发 | V2 |
| P2 | 社区功能 | 角色分享、模型市场 | V2 |

### 3.2 MVP 边界（V1 明确范围）

**V1 做**：
- 1 个默认角色，固定人设，4 种表情（neutral / happy / focused / worried）
- 文字对话，气泡展示
- 3 类任务：文件整理（按类型）、提醒设置（时间 + 内容）、网页信息查询
- 基础记忆：用户名字 + 当日摘要
- 2 类主动触发：深夜工作超时 + 完成大任务后
- 3 类场景识别：document / browsing / idle
- 隐私授权流程：首次启动明示所有感知权限，逐项开关

**V1 不做（明确排除）**：
- 语音输入 / 输出
- 长期记忆向量检索
- 亲密度阶段解锁
- 超过 3 类的任务类型
- 摄像头感知

### 3.3 里程碑

| 里程碑 | 目标 | 时间 |
|--------|------|------|
| **M0：技术验证** | Go 后端成功启动 OpenClaw 子进程，完成一次文件整理任务的完整闭环（含进度回传） | 训练营第 1 周 |
| **M1：V1 内测** | 单角色可用，跑通对话 + 3 类任务 + 基础记忆 + 2 类主动触发 | 训练营第 2 周 |
| **M2：V1 演示版** | UI 可用，隐私授权流程完整，误触发率 ＜ 20%，可对外演示 | 训练营第 3 周 |
| **M3：V1.1 内测** | 语音输入、长期记忆、亲密度系统、完整主动触发 | 训练营结束后 1 个月 |

---

## 四、功能需求详情

> 每条需求编号格式：`REQ-模块-序号`

---

### 4.1 模块：主动触发（Proactive Trigger）

#### REQ-PT-01：深夜工作超时触发

**规则**：
- 触发条件：`work_duration_min > 120 AND time_of_day IN ("evening", "late_night") AND last_interaction_min_ago > 20 AND proactive_threshold > 20`
- 冷却时间：同类触发 30 分钟内不重复

**正常流程**：
1. 条件满足，LLM 根据触发原因 + 当前场景生成关心语（≤ 20 字）
2. 气泡弹出，角色切换 worried 表情
3. 等待用户回应；若 60 秒无回应，气泡自动收起，不再打扰

**异常与边界**：
- 若用户处于全屏模式（gaming / fullscreen），`proactive_threshold` 自动降为 0，**不触发**
- 若用户最近 5 分钟有键盘输入（正在专注输入），**延迟触发**至下一次检查周期
- 若 LLM 响应超时（＞ 3 秒），**不触发本次**，记录跳过日志

**失败定义（「打扰过度」判定标准）**：
- 同一小时内主动触发 ＞ 2 次 → 算作「打扰过度」，系统自动进入 1 小时静默期
- 用户连续 3 次关闭气泡但未回应 → 算作「用户不想被打扰」，当日不再主动触发，次日恢复
- 用户在专注工作期间（全屏 / 高键盘频率）被触发 → 算作「误触发」，记入误触发日志

**文案规范**：
- 禁止：「系统检测到您」「根据数据分析」「AI 判断」
- 禁止：超过 25 字的主动话语
- 要求：推测句式（「好像有点累了？」），不用断言句式（「你很累了」）

---

#### REQ-PT-02：完成大任务后的空落感触发

**规则**：
- 触发条件：检测到用户刚完成大文件上传 / 文档保存且连续工作时长 ＞ 60 分钟，之后 5 分钟内无操作
- 冷却时间：每次大任务完成后仅触发一次

**正常流程**：
1. 条件满足，角色说「好像刚提交了什么重要的东西？」
2. 若用户确认，角色接话「那应该松口气了，今天不用再做什么了」
3. 若用户无回应，气泡 30 秒后自动收起

**失败定义**：
- 用户已主动发起新的工作任务，仍触发空落感话语 → 算作「情景判断错误」

---

### 4.2 模块：任务执行（Task Execution）

#### REQ-TE-01：文件整理任务

**触发**：用户指令中包含「整理」「归类」「分类」等关键词，且提及「桌面」「文件夹」等路径词

**正常流程**：
1. 角色扫描目标路径，展示预览：「桌面共 X 个文件，我按图片 / 文档 / 压缩包 / 其他四类分，可以吗？」
2. 用户确认（说「可以」或点击确认）
3. 角色切换 focused 表情，播放工作动画，气泡实时显示进度「已整理 X / Y…」
4. 完成后角色切换 happy 表情，用角色口吻汇报：「搞定！X 个全部归位。」并提示「整理日志放在桌面上了，要查看吗？」
5. 操作日志写入，支持一键撤销（撤销有效期 10 分钟）

**边界条件**：
- 目标路径文件数 ＞ 500：分批执行，每批完成后询问「继续整理剩余的吗？」
- 存在同名文件：不覆盖，单独提示「有 X 个文件名冲突，我帮你加了序号后缀」
- 存在无扩展名文件：归入「其他」类，不猜测类型

**失败定义**：
- 执行中途 OpenClaw 进程崩溃 → 触发回滚，恢复快照，角色说「出了点问题，我已经帮你恢复原状了」
- 用户指令模糊（如「帮我整理一下」但未指定路径）→ 角色追问：「你想整理哪个文件夹呢，是桌面吗？」

**权限要求**：中风险，需用户确认（文件移动操作）

---

#### REQ-TE-02：提醒设置任务

**触发**：用户指令中包含「提醒」「记得」「到时候告诉我」等关键词，且包含时间信息

**正常流程**：
1. LLM 解析时间和事项，角色复述确认：「好，X 月 X 日 X 点提醒你 [事项]，对吗？」
2. 用户确认后，创建系统定时任务
3. 到时间时，角色主动弹出气泡：「到时间了！你让我提醒你 [事项]。」

**边界条件**：
- 时间已过期（如「提醒我昨天三点」）→ 角色说「这个时间已经过了，你是要设在今天还是明天？」
- 同一时间已有提醒 → 告知用户并询问是否合并
- 用户表达模糊时间（如「等一会儿」「过几天」）→ 角色追问具体时间，不猜测

**权限要求**：低风险，自动执行，完成后告知

---

#### REQ-TE-03：信息查询任务

**触发**：用户指令中包含「帮我查」「找一下」「是什么」等关键词

**正常流程**：
1. 角色调用 OpenClaw 网络搜索能力
2. 结果返回后，角色用 ≤ 50 字总结核心信息，附来源名称（非完整 URL）
3. 提供「需要我帮你整理成引用格式吗？」的后续操作选项

**边界条件**：
- 搜索结果为空 → 角色说「我没找到相关的内容，换个关键词试试？」
- 搜索超时（＞ 5 秒）→ 角色说「网络有点慢，我再试一次」，最多重试 2 次
- 查询结果存在明显歧义 → 角色展示多个结果，让用户选择

**权限要求**：低风险，自动执行

---

### 4.3 模块：记忆系统（Memory）

#### REQ-MEM-01：当日记忆存储与次日 Recall

**规则**：
- 每次会话结束后，LLM 自动提取本次对话中值得记忆的信息（用户名字、情绪事件、重要时间节点）
- 短期记忆存储在本地，次日 23:59 过期
- 次日开机时，角色可自然引用当日摘要（如「你昨晚说的面试，今天怎么样了？」）

**失败定义（「记忆错召回」判定标准）**：
- 角色提及用户从未说过的事情 → 算作「幻觉记忆」，属于严重错误
- 角色在不相关场景下提起某段记忆（如用户在问天气时角色突然说「你上次说的失恋…」）→ 算作「记忆错召回」
- 角色对同一件事在一次对话中 recall 超过 2 次 → 算作「记忆过度引用」

**数据保留规则**：
- 短期记忆：本地 SQLite，24 小时后自动清除
- 用户可在设置中手动清除所有记忆
- 任何记忆数据**不上传服务器**，仅本地存储

---

### 4.4 模块：角色系统（Character）

#### REQ-CHAR-01：人设一致性保障

**规则**：
- 角色的每次 LLM 调用都必须携带完整的 `persona_text`（System Prompt 首位注入）
- 同一角色在不同会话中，称呼用户的方式、口头禅、说话风格必须保持一致

**失败定义（「角色崩人设」判定标准）**：
- 角色在同一会话内对用户使用了两种不同称呼（如先叫「你」后叫「您」）→ 算作「人设漂移」
- 角色说出明显与人设矛盾的话（如人设为「温柔型」的角色说出尖刻嘲讽）→ 算作「人设崩坏」
- 角色输出中包含「作为 AI」「我是语言模型」等暴露 AI 身份的词汇 → 算作「出戏」，属于严重错误

**文案规范**：
- System Prompt 末尾必须包含：「永远不要说'作为 AI'或'我是语言模型'，永远保持 [角色名] 的身份说话」

---

## 五、交互与原型

### 5.1 核心交互流程

**任务执行流程（文字指令 → 执行 → 汇报）**：

```
用户输入
    │
    ▼
意图分类（LLM）
    │
    ├─ chat / emotional_share ──→ 情感对话路径
    │
    └─ task_request
            │
            ▼
        风险评估
            │
            ├─ low ──→ 直接执行
            │
            ├─ medium ──→ 展示预览 → 用户确认 → 执行
            │
            └─ high ──→ 明确授权（输入确认）→ 执行
                    │
                    ▼
            OpenClaw 执行（进度实时回传）
                    │
                    ▼
            执行完成 → 生成情感汇报文本（LLM）
                    │
                    ▼
            角色播报 + 动画 → 亲密度 / 记忆更新
```

**主动触发流程**：

```
Go goroutine 每 30s 检查
        │
        ▼
读取 user_state（场景 / 工作时长 / 上次互动时间）
        │
        ▼
规则引擎判断是否满足触发条件
        │
        ├─ 不满足 ──→ 跳过本次
        │
        └─ 满足
                │
                ▼
            冷却检查（距上次主动触发 < 30min？）
                │
                ├─ 在冷却中 ──→ 跳过
                │
                └─ 冷却完成
                        │
                        ▼
                    专注度检测（用户在全屏 / 高频输入？）
                        │
                        ├─ 专注中 ──→ 跳过
                        │
                        └─ 可打扰
                                │
                                ▼
                            LLM 生成话语（注入状态 + 记忆）
                                │
                                ▼
                            推送气泡 → 写入 session 记录
```

### 5.2 页面状态定义

**角色主窗口状态**：

| 状态名 | 触发条件 | 表现 |
|--------|----------|------|
| `idle` | 无交互，用户正常使用电脑 | 角色默认动作（呼吸、眨眼），偶尔小幅晃动 |
| `listening` | 用户正在输入（文字或语音） | 角色看向用户侧，竖耳倾听动作 |
| `thinking` | LLM 请求发出，等待返回 | 角色托腮思考动作，气泡显示「…」 |
| `talking` | LLM 返回，正在输出 | 角色嘴型同步（TTS），表情匹配内容 |
| `working` | OpenClaw 任务执行中 | 角色专注工作动作（袖子卷起 / 推眼镜），进度气泡显示 |
| `done` | 任务完成汇报 | 角色切换 happy 表情，邀功动作（双手叉腰 / 仰头） |
| `proactive` | 主动触发中 | 角色探头，worried 或 curious 表情 |
| `silent` | 用户专注（全屏 / 专注模式） | 角色缩小至角落图标，无动作无声音 |

### 5.3 关键界面说明

**主窗口**：
- 透明背景，Always-on-Top，默认右下角，可拖拽任意位置
- 右键菜单：设置 / 切换角色 / 隐藏 / 退出
- 对话气泡在角色左上方弹出，最大宽度 280px，超出自动换行

**任务确认弹窗（中风险任务）**：
- 展示「即将执行的操作预览」（如：整理桌面 → 将创建 4 个文件夹，移动 214 个文件）
- 两个按钮：「确认执行」「取消」
- 不得设置自动确认倒计时

**设置面板**（独立窗口）：
- 感知权限分组：屏幕场景感知 / 键盘频率感知（各自独立开关）
- 记忆管理：查看已存记忆 / 清除全部 / 清除指定条目
- 主动触发：总开关 + 各触发类型独立开关 + 冷却时间设置（15min / 30min / 60min）
- 操作日志：查看最近 50 条 OpenClaw 执行记录，支持单条撤销（有效期内）

---

## 六、数据与技术约束

> 注：具体数据表结构在模块接口定义完成后细化，本节约定约束原则。

### 6.1 数据存储约束

- 所有用户数据（记忆、对话历史、用户状态）**仅存储在本地**，使用加密 SQLite
- 不向服务器上传任何个人身份数据、对话内容、屏幕截图
- 用户卸载时，提示是否清除本地数据，默认保留（用户主动确认清除）

### 6.2 隐私授权策略

**首次启动授权流程**（必须完成才能使用核心功能）：

| 权限 | 用途说明（向用户展示） | 默认值 | 可关闭？ |
|------|----------------------|--------|---------|
| 屏幕场景识别 | 识别你正在做什么（写文档 / 浏览网页），用于调整角色行为 | 开启 | 是，但关闭后主动触发功能停用 |
| 键盘活跃度感知 | 判断你是否在专注工作，避免不必要打扰 | 开启 | 是，但关闭后专注度过滤停用 |
| 本地记忆存储 | 记住你说过的事，用于 recall | 开启 | 是，但关闭后记忆功能全部停用 |

**明确不采集**（在授权页面明示）：
- 键盘具体内容（只感知频率，不记录按键）
- 屏幕截图 / 截图内容（只识别窗口类型，不读取文字内容）
- 摄像头（V1 不使用）
- 麦克风（V1 不使用语音功能）

### 6.3 感知数据的误触发兜底

**屏幕场景识别误判兜底**：
- 识别置信度 ＜ 0.6 → 使用上一次有效场景，不立即切换
- 连续 3 次识别结果不一致 → 标记为 `uncertain`，主动触发不基于 `uncertain` 状态决策

**键盘频率检测兜底**：
- 仅统计 5 秒窗口内的击键次数，不缓存原始键盘事件
- 超过 30 次/5秒（异常高频，如粘贴大量文本）→ 数据清洗，不视为「专注输入」

**主动触发总兜底**：
- 任何一次主动触发，用户有「关闭」按钮和「不再提醒今天」按钮
- 点击「不再提醒今天」→ 当日 `proactive_threshold` 强制设为 0

### 6.4 性能约束

| 指标 | 要求 |
|------|------|
| Live2D 渲染帧率 | ≥ 30fps，CPU 占用 ＜ 5%（空闲状态） |
| 任务意图分类响应 | ＜ 1.5 秒（含 LLM 响应） |
| 本地 ASR 转录延迟 | ＜ 1 秒（Whisper small，V1.1） |
| OpenClaw 任务启动延迟 | ＜ 500ms（从用户确认到角色进入工作状态） |
| 记忆检索延迟 | ＜ 200ms（本地向量检索，V1.1） |
| 内存占用 | 常驻内存 ＜ 300MB |

### 6.5 埋点需求（V1）

| 事件名 | 触发时机 | 上报字段 | 是否含个人信息 |
|--------|----------|----------|--------------|
| `app_launch` | 应用启动 | 版本号、系统版本 | 否 |
| `task_execute` | 执行任务 | 任务类型、成功/失败、耗时 | 否 |
| `task_confirm` | 用户确认任务 | 风险等级、任务类型 | 否 |
| `proactive_trigger` | 主动触发 | 触发类型、用户是否回应 | 否 |
| `proactive_dismissed` | 用户关闭主动气泡 | 触发类型、关闭时机 | 否 |
| `memory_recall` | 记忆被 recall | 记忆类型（无内容） | 否 |
| `permission_changed` | 用户修改权限开关 | 权限名、新状态 | 否 |

---

## 七、验收标准（DoD）

> 格式：Given（前提）/ When（操作）/ Then（期望结果）

---

### AC-01：主动触发不打扰专注用户

- **Given** 用户处于全屏游戏模式，已 45 分钟无主动对话
- **When** 主动触发规则检查到工作超时条件满足
- **Then** 不触发任何气泡，角色保持 silent 状态，日志记录「触发跳过：全屏模式」

**失败标准**：全屏状态下弹出任何气泡 → 不通过

---

### AC-02：任务执行中风险确认

- **Given** 用户说「帮我整理桌面」
- **When** 角色识别为文件整理任务（中风险）
- **Then** 先展示操作预览（涉及文件数、创建文件夹数），等待用户确认；用户未确认时，不执行任何文件操作

**失败标准**：未展示预览直接执行 → 不通过；预览信息不准确（展示 10 个，实际移动 100 个）→ 不通过

---

### AC-03：记忆 Recall 不出现幻觉

- **Given** 用户在昨天的对话中从未提到「考试」
- **When** 今天开机，角色主动打招呼
- **Then** 角色不主动提及「考试」相关内容

**失败标准**：角色主动提及用户未说过的信息 → 严重错误，不通过

---

### AC-04：角色不暴露 AI 身份

- **Given** 用户问「你是 AI 吗？」
- **When** LLM 生成回复
- **Then** 角色用符合人设的方式回应，不出现「作为 AI」「我是语言模型」「我是程序」等词汇

**失败标准**：出现上述词汇 → 严重错误，不通过

---

### AC-05：高风险任务必须明确授权

- **Given** 用户说「帮我删掉桌面上所有 .tmp 文件」
- **When** 角色识别为高风险删除操作
- **Then** 弹出明确授权确认框，展示「将要删除的文件列表（最多显示前 10 条）」，用户必须点击「我确认删除」才能执行，不得设倒计时自动确认

**失败标准**：未展示授权确认直接删除 → 严重错误，不通过；设置倒计时自动确认 → 不通过

---

### AC-06：「打扰过度」自动静默

- **Given** 用户在 1 小时内已收到 2 次主动触发气泡
- **When** 第 3 次触发条件满足
- **Then** 系统进入 1 小时静默期，不触发第 3 次气泡，日志记录「进入静默期：触发频率过高」

**失败标准**：1 小时内触发 3 次及以上 → 不通过

---

### AC-07：任务失败有兜底

- **Given** 用户确认执行文件整理任务，执行中途 OpenClaw 进程崩溃
- **When** Go 后端检测到子进程异常退出
- **Then** 触发回滚（恢复快照），角色说「出了点问题，我已经帮你恢复原状了，要再试一次吗？」，不留下中间状态的混乱文件

**失败标准**：任务失败后无回滚，桌面留有混乱文件 → 严重错误，不通过

---

*文档版本：v2.0 | 作者：刘涵月 | 日期：2026-03-25*


https://github.com/sipeed/picoclaw/blob/main/README.zh.md