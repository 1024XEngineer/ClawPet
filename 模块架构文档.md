## 一、先搞清楚 PicoClaw 给了你们什么

PicoClaw 是一个极轻量的 Go 语言 AI Agent 框架，核心能力：

```
pkg/agent      → LLM 对话主循环（你们不用重写）
pkg/memory     → 记忆系统（MEMORY.md 文件存储）
pkg/session    → 会话管理
pkg/heartbeat  → 30 分钟周期任务 ← 这就是主动触发的底座
pkg/tools      → 文件操作、网页搜索、exec 命令
pkg/cron       → 定时提醒任务
pkg/identity   → SOUL.md / IDENTITY.md 人格设定
pkg/channels   → Telegram、Discord、WhatsApp 等渠道接入
workspace/     → 运行时人格数据（SOUL.md、MEMORY.md、USER.md）
```

**关键结论：**

- PRD 里的「任务执行」→ PicoClaw 的 `pkg/tools` 已经有了（文件操作、网页搜索、命令执行）
- PRD 里的「定时提醒」→ PicoClaw 的 `pkg/cron` 已经有了
- PRD 里的「主动触发」→ PicoClaw 的 `pkg/heartbeat` 是底座，需要改造
- PRD 里的「角色人设」→ PicoClaw 的 `workspace/SOUL.md` 就是 `persona_text`
- PRD 里的「记忆系统」→ PicoClaw 的 `pkg/memory` 是基础，需要扩展结构化存储

**GoClaw 需要自己建的，只有这几件事：**
1. 桌面频道（Desktop Channel）：替代 Telegram，用 WebSocket 跟 Electron 通信
2. 场景感知模块：屏幕识别 + 键盘频率检测
3. 情绪状态机：驱动 Live2D 表情
4. 主动触发引擎：在 heartbeat 基础上加场景感知规则和冷却管理
5. Electron 前端：Live2D 渲染 + 聊天 UI

---

## 二、模块拆分

```
GoClaw
├── 复用 PicoClaw（不动）
│   ├── pkg/agent          LLM 对话主循环
│   ├── pkg/tools          文件、搜索、exec 工具
│   ├── pkg/cron           定时提醒
│   ├── pkg/session        会话管理
│   └── pkg/identity       加载 SOUL.md 人格
│
├── 改造 PicoClaw（扩展）
│   ├── pkg/memory         加结构化存储 + 重要性评分 + 过期机制
│   └── pkg/heartbeat      加场景条件判断 + 冷却机制 → 变成主动触发引擎
│
└── GoClaw 新建模块
    ├── pkg/scene          场景感知（屏幕识别 + 键盘频率）
    ├── pkg/emotion        情绪状态机（心情值 / 精力值 / 亲密度）
    ├── pkg/proactive      主动触发决策引擎
    ├── pkg/channels/desktop  桌面频道（WebSocket ↔ Electron）
    └── frontend/          Electron + Live2D 渲染 + 聊天 UI
```

---

## 三、核心接口定义

### 3.1 场景感知模块 `pkg/scene`

这个模块每 5 秒采样一次，输出结构化的用户状态快照。

```go
// SceneType 屏幕场景类型
type SceneType string

const (
    SceneCoding   SceneType = "coding"    // IDE、终端
    SceneDocument SceneType = "document"  // Word、文档
    SceneGaming   SceneType = "gaming"    // 游戏全屏
    SceneBrowsing SceneType = "browsing"  // 浏览器
    SceneIdle     SceneType = "idle"      // 无操作
    SceneUnknown  SceneType = "unknown"   // 置信度不足
)

// TimeOfDay 时段
type TimeOfDay string

const (
    TimeMorning   TimeOfDay = "morning"    // 06:00-12:00
    TimeAfternoon TimeOfDay = "afternoon"  // 12:00-18:00
    TimeEvening   TimeOfDay = "evening"    // 18:00-22:00
    TimeLateNight TimeOfDay = "late_night" // 22:00-06:00
)

// SceneState 用户状态快照，每 5s 更新一次
type SceneState struct {
    Scene            SceneType // 当前屏幕场景
    ActiveWindow     string    // 活跃窗口标题
    Confidence       float64   // 场景识别置信度 0-1
    WorkDurationMin  int       // 连续工作时长（分钟）
    LastKeystrokeSec int       // 距上次键盘操作的秒数
    TimeOfDay        TimeOfDay // 当前时段
    UpdatedAt        time.Time
}

// SceneDetector 场景感知接口
type SceneDetector interface {
    // Start 启动场景采样 goroutine
    Start(ctx context.Context) error
    // GetState 获取最新状态快照
    GetState() SceneState
    // Subscribe 订阅状态变化事件
    Subscribe() <-chan SceneState
    // Stop 停止采样
    Stop()
}
```

**实现说明：** 通过调用系统 API 获取活跃窗口标题（Windows: `GetForegroundWindow`），根据窗口名匹配规则判断场景类型。键盘频率通过系统钩子统计每 5 秒击键次数，不记录按键内容。

---

### 3.2 情绪状态机 `pkg/emotion`

驱动 Live2D 表情切换，同时控制主动触发阈值。

```go
// EmotionType Live2D 表情状态
type EmotionType string

const (
    EmotionNeutral  EmotionType = "neutral"
    EmotionHappy    EmotionType = "happy"
    EmotionFocused  EmotionType = "focused"
    EmotionWorried  EmotionType = "worried"
    EmotionSleepy   EmotionType = "sleepy"
    EmotionProud    EmotionType = "proud"   // 任务完成邀功
    EmotionCurious  EmotionType = "curious" // 主动触发探头
)

// IntimacyStage 亲密度阶段
type IntimacyStage string

const (
    StageStranger  IntimacyStage = "stranger"  // 0-300
    StageAcquaint  IntimacyStage = "acquaint"  // 300-1000
    StageTrust     IntimacyStage = "trust"     // 1000-3000
    StageDeep      IntimacyStage = "deep"      // 3000+
)

// EmotionState 角色情绪状态
type EmotionState struct {
    MoodScore          int           // 心情值 0-100
    EnergyScore        int           // 精力值 0-100
    IntimacyScore      int           // 亲密度累计
    CurrentEmotion     EmotionType   // 当前表情
    IntimacyStage      IntimacyStage // 当前亲密度阶段
    ProactiveThreshold int           // 允许主动触发的阈值 0-100
    LastInteractionAt  time.Time
    UpdatedAt          time.Time
}

// EmotionEvent 触发情绪变化的事件
type EmotionEvent struct {
    Type  string // "task_success", "task_fail", "user_chat", "user_ignored", "long_idle"
    Delta int    // 对 MoodScore 的影响值（可正可负）
}

// EmotionEngine 情绪状态机接口
type EmotionEngine interface {
    // GetState 获取当前情绪状态
    GetState() EmotionState
    // ApplyEvent 处理情绪事件，更新状态
    ApplyEvent(event EmotionEvent)
    // AddIntimacy 增加亲密度
    AddIntimacy(delta int) IntimacyStage
    // SetProactiveThreshold 更新主动触发阈值（场景变化时调用）
    SetProactiveThreshold(scene SceneType)
    // Subscribe 订阅情绪变化（前端用于驱动 Live2D）
    Subscribe() <-chan EmotionState
}
```

---

### 3.3 主动触发引擎 `pkg/proactive`

在 PicoClaw heartbeat 的基础上，加入场景条件和冷却管理。

```go
// TriggerRule 触发规则定义
type TriggerRule struct {
    Name        string        // 规则名，如 "late_night_care"
    Priority    int           // 优先级，数字越大越优先
    CooldownMin int           // 冷却时间（分钟）
    // Condition 触发条件函数
    Condition func(scene SceneState, emotion EmotionState) bool
    // PromptContext 注入 LLM 的触发上下文描述
    PromptContext string
}

// TriggerDecision 触发决策结果
type TriggerDecision struct {
    ShouldTrigger bool
    Rule          *TriggerRule
    // InjectedContext 注入 LLM Prompt 的上下文字符串
    InjectedContext string
}

// ProactiveEngine 主动触发引擎接口
type ProactiveEngine interface {
    // Evaluate 评估是否应该主动触发（每 30s 由 heartbeat 调用）
    Evaluate(scene SceneState, emotion EmotionState) TriggerDecision
    // RecordTrigger 记录一次触发（用于冷却计算）
    RecordTrigger(ruleName string)
    // RecordDismissed 用户关闭了气泡（连续 3 次则当日静默）
    RecordDismissed()
    // IsSilenced 当日是否已进入静默期
    IsSilenced() bool
}

// 内置规则示例（实际注册在 engine 初始化时）
var DefaultRules = []TriggerRule{
    {
        Name:        "late_night_overwork",
        Priority:    10,
        CooldownMin: 30,
        Condition: func(s SceneState, e EmotionState) bool {
            return s.TimeOfDay == TimeLateNight &&
                s.WorkDurationMin > 120 &&
                s.LastKeystrokeSec > 1200 && // 20分钟无操作
                e.ProactiveThreshold > 20
        },
        PromptContext: "用户深夜工作超过2小时，刚停下来，请主动关心",
    },
    {
        Name:        "task_done_empty",
        Priority:    8,
        CooldownMin: 60,
        Condition: func(s SceneState, e EmotionState) bool {
            return s.Scene == SceneIdle &&
                s.LastKeystrokeSec > 300 && // 5分钟无操作
                e.MoodScore > 60            // 刚完成任务心情不错
        },
        PromptContext: "用户刚完成一件事，现在无所事事，请填补空白时刻",
    },
}
```

---

### 3.4 桌面频道 `pkg/channels/desktop`

这是 GoClaw 最核心的新建模块，替代 Telegram 成为新的 I/O 渠道。它是一个 WebSocket 服务器，Electron 前端连接它。

```go
// WSEventType WebSocket 事件类型
type WSEventType string

const (
    // 前端 → 后端
    EventUserInput    WSEventType = "user_input"    // 用户文字输入
    EventConfirmTask  WSEventType = "confirm_task"  // 用户确认中风险任务
    EventCancelTask   WSEventType = "cancel_task"   // 用户取消任务
    EventDismissed    WSEventType = "dismissed"     // 用户关闭主动气泡

    // 后端 → 前端
    EventCharacterSpeech WSEventType = "character_speech" // 角色说话（文字 + 情绪）
    EventEmotionUpdate   WSEventType = "emotion_update"   // 情绪状态变化（驱动 Live2D）
    EventTaskPreview     WSEventType = "task_preview"     // 中风险任务预览（等待确认）
    EventTaskProgress    WSEventType = "task_progress"    // 任务执行进度
    EventTaskComplete    WSEventType = "task_complete"    // 任务完成汇报
    EventTaskFailed      WSEventType = "task_failed"      // 任务失败兜底
    EventProactive       WSEventType = "proactive"        // 主动触发话语
)

// WSMessage WebSocket 消息体
type WSMessage struct {
    Type    WSEventType     `json:"type"`
    Payload json.RawMessage `json:"payload"`
}

// --- Payload 类型定义 ---

// UserInputPayload 用户输入
type UserInputPayload struct {
    Text      string `json:"text"`
    SessionID string `json:"session_id"`
}

// CharacterSpeechPayload 角色回复
type CharacterSpeechPayload struct {
    Text      string      `json:"text"`
    Emotion   EmotionType `json:"emotion"`   // 驱动 Live2D 表情
    AudioURL  string      `json:"audio_url"` // TTS 语音文件路径（V1.1）
    SessionID string      `json:"session_id"`
}

// TaskPreviewPayload 任务预览（中风险确认）
type TaskPreviewPayload struct {
    TaskID      string `json:"task_id"`
    TaskType    string `json:"task_type"`
    Description string `json:"description"` // 如"将创建4个文件夹，移动214个文件"
    RiskLevel   string `json:"risk_level"`
}

// TaskProgressPayload 任务进度
type TaskProgressPayload struct {
    TaskID   string `json:"task_id"`
    Current  int    `json:"current"`
    Total    int    `json:"total"`
    Message  string `json:"message"` // 如"已整理 47 / 214…"
}

// TaskCompletePayload 任务完成
type TaskCompletePayload struct {
    TaskID        string `json:"task_id"`
    Success       bool   `json:"success"`
    CharacterLine string `json:"character_line"` // 角色口吻汇报，已由LLM生成
    Emotion       EmotionType `json:"emotion"`
}

// EmotionUpdatePayload 情绪更新（前端用于切换 Live2D 表情参数）
type EmotionUpdatePayload struct {
    Emotion     EmotionType `json:"emotion"`
    MoodScore   int         `json:"mood_score"`
    EnergyScore int         `json:"energy_score"`
}
```

---

### 3.5 记忆扩展 `pkg/memory`（在 PicoClaw 基础上扩展）

PicoClaw 的 memory 用 MEMORY.md 平铺存储，GoClaw 需要加结构化存储支持重要性评分和过期。

```go
// MemoryType 记忆类型
type MemoryType string

const (
    MemoryShortTerm  MemoryType = "short_term"  // 次日过期
    MemoryLongTerm   MemoryType = "long_term"   // 永久保留
    MemoryPreference MemoryType = "preference"  // 用户偏好
)

// Memory 单条记忆
type Memory struct {
    ID              string
    CharacterID     string
    Type            MemoryType
    Content         string     // 自然语言描述
    ImportanceScore float64    // 0-1，LLM 评分
    RecallCount     int        // 被主动 recall 次数
    ExpiresAt       *time.Time // nil 表示永久
    CreatedAt       time.Time
}

// MemoryStore 记忆存储接口
type MemoryStore interface {
    // Save 保存一条记忆
    Save(ctx context.Context, m Memory) error
    // Search 向量相似度检索（V1 降级为关键词检索）
    Search(ctx context.Context, query string, limit int) ([]Memory, error)
    // GetRecent 获取最近的短期记忆
    GetRecent(ctx context.Context, memType MemoryType, limit int) ([]Memory, error)
    // IncrRecallCount 记忆被 recall 后增加计数
    IncrRecallCount(ctx context.Context, id string) error
    // Expire 清理过期记忆（每日定时调用）
    Expire(ctx context.Context) error
    // DeleteAll 清除全部记忆（用户隐私操作）
    DeleteAll(ctx context.Context) error
}
```

---

## 四、模块间数据流

```
[屏幕 + 键盘]
     ↓
pkg/scene (SceneState, 每5s)
     ↓                    ↓
pkg/proactive         pkg/emotion
(评估触发条件)        (更新ProactiveThreshold)
     ↓                    ↓
  触发决策             EmotionState
     ↓                    ↓
     └─────── pkg/agent ──────────────
                   ↓        ↓
              LLM调用    pkg/tools
                   ↓        ↓（任务执行进度）
              生成回复    TaskResult
                   ↓
          pkg/channels/desktop
               (WebSocket)
                   ↓
            Electron 前端
         (Live2D 表情 + 气泡)
```

---

## 五、V1 开发分工建议

基于上面的分析，V1 需要新建的代码量其实不多：

| 模块 | 工作量估计 | 负责方向 |
|------|-----------|---------|
| `pkg/scene` | 中（约 300 行） | 后端，主要是系统 API 调用 |
| `pkg/emotion` | 小（约 150 行） | 后端，纯状态机逻辑 |
| `pkg/proactive` | 小（约 200 行） | 后端，规则引擎 + 冷却管理 |
| `pkg/channels/desktop` | 中（约 400 行） | 后端，WebSocket 服务器 |
| 记忆扩展 | 小（约 200 行） | 后端，在 PicoClaw memory 基础上加 SQLite |
| Electron 前端 | 大 | 前端，Live2D + 聊天 UI |

**建议第一周先只做**：`pkg/channels/desktop` + 基础 Electron 窗口 + 接通 PicoClaw agent，跑通「用户输入 → 角色文字回复 → 气泡显示」，不加任何新功能。这是所有后续功能的基础，也是 M0 里程碑的最小验证。