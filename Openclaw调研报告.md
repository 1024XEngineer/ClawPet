### 我对Openclaw的理解

- Openclaw 是一个被赋予了系统最高权限的 AI 工具， 本质上是赋予了大模型操作电脑的能力，用户发布任务，然后由大模型决策并执行。优点是可以由用户自由配置Skill和具有记忆机制，Skill解决了让网页大模型执行任务前每次都要重复编写提示词的问题，记忆机制解决了网页大模型会失忆的问题。并且Openclaw可以全天候自主运行，不需要像给网页大模型布置任务一样一直盯着屏幕操作。缺点是安全存在隐患。


### Openclaw 的核心功能

1. 全天自主运行
-  不需要一直盯着屏幕操作，即使离开电脑或者睡觉，它也能在后台持续运行（保持电脑开机）

2. 支持多平台通讯网关
-  如飞书、Telegram、Discord等
-  在外面通过平台给Openclaw发送指令，Openclaw就会在家里的电脑自动执行

3. 接管设备与浏览器
-  拥有电脑的最高权限，可以接管电脑上的摄像头、麦克风、扬声器等设备，也可以控制浏览器，自动打开网页、填写表单等

4. 拥有长期记忆
-  它会根据你的日常交互不断学习你的习惯、工作流，并把这些知识保存到本地的MEMORY.md文件中，不断优化性格与行为

5. 可无限扩展的技能（Skill）
-  Skill是指导openclaw工作的指令集，用户可以自己编写Skill，也可以从社区下载，以实现Openclaw在不同场景下的功能，并保证AI工作流程的专业性

---

### Openclaw 主要功能的实现原理

1. 消息编排与网关层
-  负责处理所有输入与输出，解决了“谁在跟AI说话”和“权限管理”的问题

2. 推理层
-  Openclaw的决策中枢。通过API调用顶尖大模型
-  理解用户指令，并进入循环，在循环内将用户指令拆解成多个步骤，并自主决定每一步要调用的工具，按顺序执行

3. 记忆系统
-  MEMORY.md文件，记录了Openclaw的日常交互，并不断优化，以实现长期记忆
-  当对话达到一定长度，触发“摘要与提炼”动作，将关键的人物画像、偏好、工作流知识提取出来，写入本地的 Soul.md。下次交互会先提取这些记忆作为前置提示词

4. 技能与沙盒层
-  通过Skill保证AI工作流程的专业性
-  沙盒层负责执行Skill，并保证Skill的执行不会对电脑造成损害

---

### Openclaw 后台执行顺序

1. 网关层
- 接受消息 -> 身份确认 -> 消息编排 -> 打包发送给推理层

2. 记忆系统
- 提取Soul.md -> 提取MEMORY.md -> 摘要与提炼 -> 生成前置提示词

3. 推理层
- 解析指令 -> 拆解指令 -> 生成执行步骤 

4. 技能与沙盒层
- 按顺序执行 -> 生成反馈 -> 将抓取到的数据返回给推理层

5. 网关层
- 推理层拿到结果后，将结果打包发送给网关层 -> 网关层将结果发送给用户

---

### Openclaw 使用体验

- 由于今天早上一直到下午两点都在配置Openclaw网络，调用Gemini模型每次都报错LLM超时，所以没有实际体验Openclaw的功能。

- 现在放弃一定要调用Gemini模型，改为调用国内模型


### 🌐 尝试过的网络配置

1. **应用层：环境变量代理 (`.env` 注入)**
   * **操作**：配置 `HTTP_PROXY`、`HTTPS_PROXY`，试图让 OpenClaw 主动走你的 7898 代理端口。
   * **进阶**：加入了 `NO_PROXY=127.0.0.1`，成功解决了代理产生的“内循环死锁”，让你重新打得开 Dashboard 面板。
2. **网络层：强制 IPv4 寻址**
   * **操作**：注入 `NODE_OPTIONS=--dns-result-order=ipv4first`。
   * **目的**：阻止 Node.js 默认走 IPv6 通道，防止流量在不支持 IPv6 的梯子或校园网里变成“黑洞”。
3. **系统层：全局虚拟网卡接管 (TUN 模式)**
   * **操作**：开启 VPN 的 TUN 模式。
   * **目的**：放弃让程序主动走代理，改为在 Windows 系统底层强制拦截所有流量送入 VPN。
4. **进程层：暴力网络劫持 (Proxifier - 未遂)**
   * **操作**：准备用 Proxifier 直接锁定 `node.exe` 进程，强制其所有协议走 7898 端口。
   * **结果**：因软件过期放弃
5. **接口层：API 终点“偷梁换柱” (当前进度)**
   * **操作**：试图通过环境变量 `OPENAI_BASE_URL`，把原本发往海外 OpenAI 的流量，在国内半路截胡并转向给你充了钱的 DeepSeek。

---

### 🕵️‍♂️ 排除了哪些问题

1. **🚫 不是 VPN 节点不行（节点完全健康）**
   * **证据**：你在浏览器直连 `generativelanguage.googleapis.com` 得到了 `404 Not Found`。这证明节点不仅没有被 Google 拉黑，而且速度和连通性非常完美。
2. **🚫 不是操作失误（配置全部生效）**
   * **证据**：加上 `NO_PROXY` 后你的面板恢复了；日志里清晰打印出了网络请求的状态。你的每一步命令行操作和文件修改都是绝对精准的。
3. **🚫 不是 OpenClaw 软件坏了（核心功能正常）**
   * **证据**：日志里出现了 `moonshot/kimi-k2.5` 的 `rate limit`（限流）提示，说明 OpenClaw 是有能力发包和收包的，只是被接口卡了。

### Gemini分析的失败原因：
* **Node.js 18+ 的底层机制**：它使用了极度固执的原生 `fetch`，天生无视系统的 `HTTP_PROXY` 变量。


---

### 网络上总结的使用体验
- 由于目前Openclaw的Gemini模型调用失败，所以目前没有实际体验Openclaw的功能，但是网络上总结的使用体验如下：

- 极度烧钱（Token 刺客）
- 记忆库混乱，上下文易错乱
- AI 经常“假装干活”（宣称完成任务但实际未执行）。
- 缺乏执行反馈与透明度（无法直观看到 AI 在后台执行了哪些操作）
- 存在安全风险
- 用户体验差（操作复杂，需要手动配置环境变量，安装依赖等）
- 只适用于高度重复的脏话累活，不适合进行低容错率的任务

---

### 希望设计的 Openclaw 产品

- 安全性问题：把Openclaw的权限限制在只能操控某一个文件夹以内，并且只能执行某些指令，防止Openclaw对电脑造成损害。

### 痛点：当前大学生和自学者的核心矛盾：
**我们信任书本的权威，但我们的大脑却更偏好视频的轻松；我们想要网课的直观，却又苦于网课与手头教材的错位。**

---

# 📄 项目提案：《基于 Openclaw 的教材全自动“网课化” Agent》

## 一、 产品背景与痛点分析
当前知识获取领域存在三个极度割裂的痛点，也是本项目的核心解决目标：

1. **教材与网课的“错位”痛点：** 学生在自学或复习时，往往只能在网上搜索通用的教学视频。这些视频的知识体系、章节编排与学生手中正在使用的具体教材（如指定版本的《计算机网络》）存在出入，导致复习效率低下，难以精准对应考点。

2. **“厚书恐惧症”与视频化偏好：** 面对动辄大几百页的专业大部头，用户普遍存在畏难情绪，难以坚持阅读。我就买了很多计算机相关的专业书籍，动辄好几百也，我买完都不想翻开，一个是没有做好开始学习的几百页的厚书的准备，另一个是拿不出特别整块的时间来阅读。在当前的媒介环境下，人们已经养成了“刷视频”的习惯，更倾向于通过高视听刺激的视频来获取知识，而不是枯燥地啃文字。

3. **知识的“权威性”危机：** 网上搜到的野生教学视频良莠不齐，UP 主的专业水平无法保证，知识存在错漏风险。相比之下，正式出版的专业书籍拥有绝对的权威性。**用户渴望一种既拥有“书本级权威准确度”，又具备“视频般轻松体验”的媒介形态。**
有些博主分享的信息消息来源不明，有可能是胡编乱造，网络上经常能看见打假的视频；有些中专学历的博主高谈阔论，有几十上百万的播放，有些博主是门外汉，他分享的观点是否值得参考，有些人穿上医生的白大衣就满嘴跑火车，输出离谱的医学观点误导观众，我妈就很爱看这种，还很喜欢相信。总的来说就是看视频的人无法判断视频创作者的专业水平，但正式出版的专业书籍就很权威，转换成视频的形式，既保留了书本的权威性，又保留了看视频学习的轻松体验，这就是本项目要解决的问题。

## 二、 核心解决方案
本项目旨在开发一款基于 Go 语言和 Openclaw 的桌面级自动化 Agent。用户只需上传本地的权威电子教材（PDF/TXT），Agent 即可全自动将其转化为高质量的、完美贴合原书章节的“专属网课视频”。

## 三、 技术实现路径（Openclaw 与大模型的完美分工）
本项目的技术亮点在于明确区分了“认知大脑”与“操作双手”，完美契合 Openclaw 的核心能力（UI 自动化操控），并非虚无缥缈的纯 AI 视频生成：

* **大脑层（大模型数据处理）：采用“双轨拆解”架构。**
  调用大语言模型（如 Kimi/ChatGPT），将传入的教材文本解析为结构化数据：
  * **轨道 A（画面骨架）：** 提取极简的核心概念、小标题，用于生成 PPT 画面文字。
  * **轨道 B（语音血肉）：** 将枯燥的书面语扩写为口语化、生动且保留原书 100% 知识深度的“讲课逐字稿”，用于生成配音。
* **执行层（Openclaw 自动化操控）：模拟人类助教工作流。**
  利用 Openclaw 的视觉识别和键鼠操控能力，在本地电脑完成以下串联动作：
  1. **排版操作：** 自动打开 PowerPoint，新建页面，将“轨道 A”的内容自动粘贴并套用模板。
  2. **配音操作：** 自动打开浏览器进入 TTS（文字转语音）网站，将“轨道 B”的讲课稿粘贴，生成并下载高质量的 AI 教师配音。
  3. **合成录制：** 自动将配音插入对应 PPT，随后操控 OBS 录屏软件点击“开始录制”，全屏播放 PPT 并自动翻页，最终输出标准 MP4 网课文件。

## 四、 项目优势与答辩亮点
* **技术落地性强：** 避开了难度极高的“AI 生成连贯视频画面”，将复杂问题转化为 Openclaw 最擅长的“键鼠模拟与 UI 交互”，工作流极度稳定。
* **商业价值高：** 这是一个极其完整的闭环产品，不仅是一项技术 Demo，更是一个可以直接推向学生群体、甚至能商业化运作的实用工具。


Soul.md  情绪价值？？？
